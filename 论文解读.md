# 目录

1. [CameraBench: Towards Understanding Camera Motions in Any Video](#camerabench-towards-understanding-camera-motions-in-any-video)
2. [ShotBench: Expert-Level Cinematic Understanding in Vision-Language Models](#shotbench-expert-level-cinematic-understanding-in-vision-language-models)
3. [RefineShot: Rethinking Cinematography Understanding with Foundational Skill Evaluation](#refineshot-rethinking-cinematography-understanding-with-foundational-skill-evaluation)
4. [CamReasoner: Reinforcing Camera Movement Understanding via Structured Spatial Reasoning](#camreasoner-reinforcing-camera-movement-understanding-via-structured-spatial-reasoning)
5. [Geometry-Guided Camera Motion Understanding in VideoLLMs](#geometry-guided-camera-motion-understanding-in-videollms)
6. [VGGT: Visual Geometry Grounded Transformer](#vggt-visual-geometry-grounded-transformer)
7. [VG-LLM: Learning from Videos for 3D World](#vg-llm-learning-from-videos-for-3d-world)
8. [On the Generalization Capacities of MLLMs for Spatial Intelligence](#on-the-generalization-capacities-of-mllms-for-spatial-intelligence)
9. [VG-LLM vs. CA-MLLM：深度对比分析](#vg-llm-vs-ca-mllm深度对比分析)
10. [ViPE: Video Pose Engine for 3D Geometric Perception](#vipe-video-pose-engine-for-3d-geometric-perception)
11. [DROID-W: DROID-SLAM in the Wild](#droid-w-droid-slam-in-the-wild)
12. [UniDepthV2: Universal Monocular Metric Depth Estimation Made Simpler](#unidepthv2-universal-monocular-metric-depth-estimation-made-simpler)
13. [VGGT-Ω: Scaling Feed-Forward Reconstruction](#vggt-ω-scaling-feed-forward-reconstruction)

---

## CameraBench: Towards Understanding Camera Motions in Any Video

**发表年份**：2025（arXiv 2025.04）

### 核心问题：现有相机运动数据集一塌糊涂

论文系统梳理了 MovieNet、AVE、DREAM-1K、VDC 等 7 个主流数据集的三大致命缺陷：

1. **定义错误**：MovieNet 把向前平移（dolly-in）定义为 zoom，混淆了外参变化与内参变化
2. **标注矛盾**：AVE 有超过 1000 个视频同时被标注为"静止（locked）"和"运动（pan/tilt）"，互相矛盾
3. **质量失控**：近期 VLM benchmark（VDC、VidComposition 等）超过 50% 的相机运动描述存在错误，例如把 tilt-down 标成 tilt-up

根本原因：相机运动的感知依赖参考系（相机坐标系 vs. 地面坐标系 vs. 物体坐标系），且 zoom（内参）和 dolly（外参）等概念对非专业人士极易混淆。

### 核心贡献 1：与电影摄影师合作制定的运动原语分类体系（50+ 原语）

历时三个月迭代，覆盖三种参考系（相机/地面/物体中心），包含：

- **运动类型**：无运动 / 简单 / 细微 / 复杂模糊
- **稳定性**：静止 / 不同程度抖动
- **平移**：dolly（前后）/ pedestal（上下）/ truck（左右）
- **旋转**：pan（左右）/ tilt（上下）/ roll（顺逆时针）
- **内参变化**：zoom in/out
- **物体中心运动**：arc（环绕）/ tail-tracking / lead-tracking / side-tracking / aerial-tracking 等
- **其他**：运动速度、dolly-zoom 效果、场景动态程度

这是首个同时覆盖几何原语和语义原语（如 tracking shot）的分类体系，而此前数据集最多只定义 4-5 类。

### 核心贡献 2：CameraBench 数据集与 Benchmark

- **规模**：~3,000 个多样化互联网视频，150K+ 高质量标注（标签 + 自然语言描述）
- **视频来源**：YouTube，覆盖自然、电影、广告、新闻、游戏、无人机、自拍、动漫等 20+ 类型，手动分镜（而非自动）
- **标注框架**："先标签后描述（label-then-caption）"——先对每个原语做二分类，遇到模糊/冲突运动时只标有把握的，再用自然语言描述复杂情况
- **质量控制**：招募 100+ 标注员（含专业摄影师），经过 5 轮考试+反馈培训，仅录用通过者；专家比新手准确率高 15%+，培训后所有人提升 10-15%
- **评测任务**：
  - 运动原语二分类（Binary Classification）
  - VQA（9 大技能 / 81 个子任务，每题配正负视频对，防止盲猜）
  - 视频文本检索（Video-Text Retrieval）
  - 运动描述生成（Captioning）

### 核心发现：SfM 和 VLM 各有盲区，互补

| 模型类型 | 优势 | 劣势 |
|---------|------|------|
| SfM/SLAM（COLMAP、MegaSAM 等） | 几何原语（平移/旋转方向）估计 | 动态场景、低视差场景（纯旋转）失败；无法理解语义原语（tracking shot） |
| 判别式 VLM（InternVideo2 等） | — | 几何和语义均弱，VQA 接近随机水平 |
| 生成式 VLM（GPT-4o、Qwen2.5-VL 等） | 语义原语（tracking shot）理解 | 几何原语（精确轨迹估计）弱 |

具体数字：最好的 SfM 方法（MegaSAM）在运动原语分类上整体 AP 约 50%，远未解决问题；所有开源 VQA 模型在 CameraBench 上表现在随机水平或以下。

### 核心发现：小规模高质量微调效果显著

用 ~1400 个训练视频（与测试集无重叠）对 Qwen2.5-VL 做 SFT：

- 运动原语分类性能提升 **1-2 倍**，达到 MegaSAM 同等水平（兼具几何和语义理解）
- VQA 所有技能均达到最优，尤其在最难的 Tracking Shot 和 Only Motion（需要物体中心推理和逻辑推理）上提升最大
- 描述生成质量（SPICE、LLM-as-a-Judge）超过 GPT-4o 和 Gemini-2.5-Pro

**核心结论**：SfM 擅长几何、VLM 擅长语义，两者互补；用高质量标注数据微调 VLM 可以同时获得两者的优势，且数据量不需要很大。

---

## ShotBench: Expert-Level Cinematic Understanding in Vision-Language Models

**发表年份**：2025（arXiv 2025.06）

### 核心问题：VLM 对电影摄影语言的理解几乎是盲区

现有 VLM benchmark 缺乏对电影摄影语言的专项评测。ShotBench 填补这一空白，评测结果触目惊心：**24 个主流 VLM 中，最强的 GPT-4o 平均准确率不到 60%**，相机运动维度超过一半的模型准确率接近随机猜测（25%）。

### 数据集构建

- **来源**：200+ 部奥斯卡最佳摄影奖提名/获奖影片，保证专业摄影质量
- **规模**：3,049 张图像 + 464 段视频片段，共 **3,572 个专家标注 QA 对**
- **覆盖 8 个维度**：镜头大小（Shot Size）、镜头取景（Shot Framing）、相机角度（Camera Angle）、镜头焦距（Lens Size）、灯光类型（Lighting Type）、灯光条件（Lighting Condition）、构图（Composition）、**相机运动（Camera Movement）**
- **标注流程**：图像数据从专业摄影参考平台 Shotdeck 提取候选标签，标注员核验；视频数据人工标注运动区间和类型；多轮专家审核，准确率 >90%

与其他 benchmark 的对比：CameraBench 只覆盖相机运动，MovieNet/MovieShots 只覆盖镜头大小和相机运动，ShotBench 是首个同时覆盖全部 8 个维度的 benchmark。

### 主要发现

**三大核心弱点**：

1. **细粒度视觉-术语对齐**：模型难以区分视觉上相邻的类别。如 Medium Shot（腰部以上）vs. Medium Close-Up（胸部以上），GPT-4o 有 36.2% 的 MS 被误判为 MCU。Wide lens vs. Long lens 的混淆率也很高。

2. **相机空间感知**：静态相机角度（low angle/high angle）GPT-4o 仅 58.2%；动态相机运动更差，超过一半模型低于 40%。典型错误：push in（相机物理前移）vs. zoom in（焦距变化），需要感知视差才能区分；tilt up（绕轴旋转）vs. boom up（相机物理上移），需要判断是旋转还是平移。

3. **电影语言推理**：如识别"short side composition"需要推断主体视线方向与画面位置的关系，需要专家级推理。

**规律**：同系列模型中，更大的模型准确率更高（规模效应存在）；开源和闭源模型整体差距不大（Qwen2.5-VL-72B 59.1% ≈ GPT-4o 59.3%）。

### ShotQA + ShotVL：针对性训练

**ShotQA**：首个大规模电影摄影理解数据集，58,140 张图像 + 1,200 段视频，共 **~70,000 个多选 QA 对**，来自 243 部影片。

**ShotVL 两阶段训练**（基座：Qwen2.5-VL-3B）：
- **Stage 1 SFT**：用 ~60k QA 对做监督微调，建立视觉特征与电影术语的对齐
- **Stage 2 GRPO**：用 ~8k 高质量样本做强化学习，提升推理能力；奖励函数为二元准确率奖励 + 格式奖励（`<think>` + `<answer>` 标签）

**结果**：ShotVL-3B 平均准确率 65.1%，比基座 Qwen2.5-VL-3B（46.1%）提升 **+19 点**，超过 GPT-4o（59.3%）和 Qwen2.5-VL-72B（59.1%）——3B 参数超越 72B。

**消融发现**：CoT-SFT（用 Gemini 生成推理链做 SFT）收益极小，因为生成的推理链质量不高引入噪声；GRPO 专注于结果奖励，效果更稳定。有趣的是，即使消融实验只用静态图像（无相机运动样本），推理增强训练也能提升相机运动维度 0.4-4.6%，说明推理能力对动态感知有隐式迁移效果。

---

## RefineShot: Rethinking Cinematography Understanding with Foundational Skill Evaluation

**发表年份**：2025（arXiv 2025.10）

### 核心问题：ShotBench 的选项设计有缺陷，ShotVL 的高分存在水分

这篇论文是对 ShotBench 和 ShotVL 的**批判性分析**，发现两个层面的问题：

**问题 1：ShotBench 选项设计不一致**

多选题的选项来自不同描述维度，违反互斥原则。以灯光条件为例，同一题目中混入了：
- 方向描述（side light、backlight、top light）
- 对比度描述（high contrast、low contrast）
- 强度描述（hard light、soft light）

这些属性来自不同维度，天然存在重叠，导致评测结果不可靠。

**问题 2：ShotVL 的推理不忠实 + 指令不遵从**

- **推理不忠实**：`<think>` 推理过程和 `<answer>` 最终答案经常矛盾——推理正确但答案错，或推理错误但答案对。用 Qwen3-2B 做一致性检验后，ShotVL-3B 准确率从 68.3% 跌到 59.0%（**-8.9 点**），而 Qwen2.5-VL 系列几乎不变（-0.7/-0.8 点）。
- **指令不遵从**：给 ShotVL-7B 提供分步推理格式要求，它直接忽略，输出重复冗长的自由文本；而 Qwen2.5-VL-7B 能严格按格式输出。ShotVL-7B 在逐步推理 prompt 下准确率从 70.2% 崩到 **34.0%**（-36.2 点），推理时间从 4 分钟暴增到 37 分钟。

### 解决方案：RefineShot

**Benchmark 修正**：对 961 个问题重新设计选项——要求所有选项来自同一子类（与正确答案同维度），随机打乱顺序消除位置偏差；当子类只有一个元素时改为二分类。

**新评测指标**：

- **Faithful Reasoning Score（FRS）**：`<think>` 结论与 `<answer>` 一致的比例，衡量推理忠实度
- **Instruction Adherence Score（IAS）**：在逐步推理 prompt 下，同时满足"格式合规"和"答案正确"的比例除以原始准确率，衡量指令遵从能力

**结果对比**：

| 模型 | 准确率 | FRS | IAS |
|------|--------|-----|-----|
| Qwen2.5-VL-3B | 47.5% | **98.5%** | **87.8%** |
| Qwen2.5-VL-7B | 51.7% | **98.9%** | **93.5%** |
| ShotVL-3B | 67.8% | 83.2% | 16.4% |
| ShotVL-7B | **70.2%** | 93.0% | 11.7% |

ShotVL 准确率高但可靠性极差（IAS 仅 11-16%）；Qwen 准确率低但推理忠实、指令遵从能力强（IAS 88-94%）。

**核心结论**：高 benchmark 分数可能掩盖推理不忠实和指令不遵从的根本缺陷。评测模型不能只看准确率，还需要评估推理一致性和指令遵从能力。

---

## CamReasoner: Reinforcing Camera Movement Understanding via Structured Spatial Reasoning

**发表年份**：2026（arXiv 2026.02，UC Merced / UQ / UCAS / UB）

### 核心问题：VLM 理解相机运动靠"猜"而非"推理"

现有 VLM 把相机运动理解当作黑盒分类——直接从像素跳到标签，依赖场景上下文（如"运动员在跑"）而非真正的几何线索。典型错误：把 zoom（内参变化）和 dolly（外参平移）混淆，因为两者视觉上都是"画面变大"，但物理机制完全不同。

### 核心思路：O-T-A 推理范式

提出 **Observation-Thinking-Answer（O-T-A）** 三段式结构化推理：

```
<observation> 描述视频中的空间-时间视觉线索（背景运动、主体位置变化等）</observation>
<think> 基于观察推断相机的实际运动类型，排除干扰因素 </think>
<answer> A/B </answer>
```

核心逻辑：先显式描述"看到了什么"，再推理"相机在做什么"，强迫模型把感知和推理解耦，而不是直接猜答案。

### 数据构建：两阶段推理轨迹套件

**CamReasoning-SFT-18k**（冷启动数据）
- 来源：CameraBench 训练集（~1,400 个视频）。**视频数量没变，但每个视频有 50+ 个运动原语，每个原语都可以问一个二分类问题**（"有没有 pan left？"、"有没有 dolly in？"……），所以 1,400 个视频 × 每视频若干原语问题 = 38,672 条初始 QA 对
- 用 Qwen2.5-VL-72B 对每条 QA 对做答案条件化生成（已知正确答案，倒推出合理推理链），生成 38,672 条初始推理链
- 用 Qwen3 做三维过滤：格式合规 + 答案准确 + 推理一致性
- 人工抽检 500 条，接受率 93%，最终保留 18,541 条高质量样本
- 数据分布：旋转 39.3%、静止 27.5%、变焦 18.3%、平移 14.9%

**CamReasoning-RL-38k**（强化学习探索数据）
- 同一 CameraBench 训练集（~1,400 个视频），同样"炸开"为 38,451 条视频-QA 对，**不含推理标注**
- 作为 RL 环境查询，让模型自己生成推理路径并通过奖励信号优化

### 训练策略：SFT 冷启动 + GRPO 强化对齐

**第一阶段：SFT**
- 基座：Qwen2.5-VL-7B-Instruct
- 冻结视觉塔和多模态投影层，只训练语言模型部分
- 目标：让模型学会生成合规的 O-T-A 格式推理链

**第二阶段：GRPO 强化学习**
- 从第 2 个 SFT epoch 的 checkpoint 初始化（第 3 个 epoch 已过拟合，输出多样性不足，GRPO 探索会崩溃）
- 奖励函数：`R = (1-λ)·准确率奖励 + λ·格式奖励`，λ=0.1
- 采用 **EMA-GRPO**：用指数移动平均代替批次级标准差做优势归一化，防止低方差 rollout 导致梯度不稳定
- 每个 prompt 采样 G=8 个 rollout，过滤奖励最高/最低 1% 的样本
- 训练动态：格式奖励在前 20 步迅速收敛到 1.0，准确率奖励从 0.68 稳步爬升到 0.85+

**计算成本**：4×H200，SFT 约 3 小时，GRPO 约 7 小时，共 10 小时

### 实验结果

在 CameraBench 上（评测协议改为准确率，而非原始 mAP）：

| 模型 | 二分类准确率 | VQA 准确率 |
|------|------------|-----------|
| Qwen2.5-VL-7B（基座） | 73.8% | 60.9% |
| GPT-4o | 66.5% | — |
| CamReasoner-7B（SFT only） | 75.8% | 72.3% |
| **CamReasoner-7B（SFT+RL）** | **78.4%** | **74.5%** |

- 在最难的"易混淆运动（Confusable Motion）"子任务上达到 60.7%，依靠结构化推理显式排除干扰运动
- 在 ShotBench（域外）上准确率 39.7%，反映训练数据覆盖不足（dolly vs. zoom 等混淆对样本稀少）
- 在 RefineShot（推理一致性评测）上表现最优，说明 O-T-A 范式显著提升了输出稳定性

### 消融实验关键结论

- **RL 初始化时机很关键**：从 Epoch 2 初始化 RL 比 Epoch 3 好（过拟合后 rollout 多样性崩塌，GRPO 优势信号消失）
- **格式奖励权重 λ=0.1 最优**：λ 过大会让模型过度关注格式而牺牲推理深度
- **EMA-GRPO 优于标准批次归一化**：动量 α=0.99 最优，过滤高频噪声同时保持对奖励分布变化的响应

---

## Geometry-Guided Camera Motion Understanding in VideoLLMs

**发表年份**：2026（arXiv 2026.03）

### 核心问题：VideoLLM 对相机运动几乎"视而不见"

在 CameraMotionVQA 上测试多个主流 VideoLLM，大多数模型准确率接近随机猜测水平（25%）。根本原因：VideoLLM 的视觉编码器在训练时优化的是语义对齐，而非几何变化，相机运动这种时空几何信号在深层 ViT 特征中几乎消失。

### 核心思路：诊断 + 注入，不修改 VLM 权重

论文提出一个"基准测试 → 诊断 → 注入"的闭环框架：
1. **建 benchmark** 量化 VideoLLM 的相机运动盲区
2. **做 probing** 找到信息在哪一层丢失
3. **外挂几何信号** 用 3D 基础模型提取相机线索，通过结构化 prompt 注入，不改 VLM 权重

### 数据集构建：CameraMotionDataset

**数据来源**：MultiCamVideo Dataset（ReCamMaster 论文发布），Unreal Engine 5 渲染的真实感动态场景，包含精确的逐帧相机内外参，共 13.6K 场景、136K 视频。

**处理流程**：
- 将每个视频切成不重叠的 **1 秒片段**，每段均匀采样 8 帧（336×336）
- 直接从外参矩阵计算每段的平移/旋转变化，映射到 15 个原子运动原语（pan-left/right、tilt-up/down、dolly-in/out、truck-left/right、pedestal-up/down、roll-CW/CCW、arc-CW/CCW、static 等）
- 允许复合运动（如 pan-left + tilt-up），但用**互斥矩阵**禁止物理上不可能的组合（如 pan-left + pan-right）
- 人工验证 720 个片段，标注一致率 93%
- 类别平衡采样后保留 **12,274 个片段**，按 80/10/10 划分训练/验证/测试集

**CameraMotionVQA**：每个 1 秒片段构造一道 4 选 1 多选题，干扰项与正确答案的标签复杂度相同（原语数量相近），且所有选项都满足互斥约束，避免答案长度偏差。

与 CameraBench 的区别：CameraBench 用真实互联网视频 + 人工标注，CameraMotionDataset 用合成视频 + 外参直接计算标签，标注更精确但存在合成-真实域差距。

### 方法 1：用 3D 基础模型提取相机线索

**每秒片段的处理流程**：

每个 1 秒片段原始帧率为 15fps（即 15 帧），从中**均匀采样 T=8 帧**，每帧 resize 到 336×336，同时输入冻结的 **VGGT**。VGGT 对每帧输出一个 2048 维相机 token，8 帧得到 8 个相机 token $\{c_1, c_2, ..., c_8\}$，已编码帧间相机位姿变化信息（VGGT 内部做了跨帧几何推理）。

随后将 8 个相机 token 送入轻量 Transformer 分类器：

```
8个相机token → 线性投影(2048→512) → 加位置编码 → 拼上[CLS]token
→ 4层Transformer编码器（跨帧注意力）→ 取[CLS]输出 → 线性层 → 15维logits
```

每秒独立预测一次，输出该秒的多标签运动原语集合，最终拼成整段视频的逐秒运动序列。

用冻结的 **VGGT** 对每帧提取 2048 维相机 token，再用一个轻量 Transformer 分类器（4 层编码器 + [CLS] token）预测多标签运动原语。

损失函数包含三项：
- **BCE 损失**：多标签二分类
- **互斥正则化**：惩罚互斥原语同时激活（如 pan-left 和 pan-right 同时为正）
- **基数正则化**：约束每段预测的原语数量在合理范围（1-3 个）

结果（CameraMotionDataset 测试集）：

| 方法 | 实例准确率 | Macro-F1 | Weighted-F1 |
|------|-----------|---------|------------|
| VGGT + 约束 | **73.8%** | **0.87** | **0.92** |
| VGGT 无约束 | 57.2% | 0.79 | 0.84 |
| VGGT-QFormer（蒸馏） | 63.8% | 0.83 | 0.87 |
| Q-Former probing（VLM特征） | 45.0% | 0.69 | 0.74 |

互斥约束带来 16% 的实例准确率提升，说明物理约束建模非常重要。

### 方法 2：Probing 诊断——相机信息在哪一层丢失

用 Q-Former 风格的探针（4 个可学习 query token + 2 层 Transformer）在 Qwen2.5-VL 视觉编码器的不同层提取特征，预测运动标签。

**发现**：性能在第一个全注意力块（第 7 层）达到峰值，之后随层数加深单调下降。越深的层越偏向语义对齐，相机运动的几何信号越来越难以恢复。这解释了为什么 VideoLLM 在相机运动识别上表现差。

### 方法 3：结构化 Prompt 注入（无需修改 VLM）

**重要说明**：**分类任务完全由 VGGT + 轻量分类器完成，VideoLLM 本身不做分类**。Prompt 注入只是把分类结果序列化为文本，前置到 VideoLLM 的输入里，让 VideoLLM 输出更好的描述。

将每秒的运动预测序列序列化为文本，前置到用户指令中：

```
Per-second camera motion: [static, pan-left, pan-left and tilt-up, ...]
Describe this video using the filmmaker's language...
```

效果：同一个 VideoLLM，加了运动头之后，描述从"相机快速平移（方向不明）"变成"先静止中景，然后向左平移跟随指挥，再向右平移切换到鼓手，保持空间连贯性"——方向明确、时序清晰、有电影语言意识。

**评估方式**：论文中**只做了定性展示，没有定量打分**。没有使用 BLEU、METEOR、SPICE、CIDEr 等指标，也没有 LLM-as-a-Judge 评分，只给了上述一个对比例子作为概念验证（proof of concept）。原因是本文的核心贡献是**诊断框架**（probing 分析 + 约束建模），Prompt 注入只是"几何信号有用"的附带验证，而非训练新的描述生成模型。相比之下，CameraBench 论文才对描述生成做了完整的定量评估（SPICE + LLM-as-a-Judge），微调后的模型超过了 GPT-4o 和 Gemini-2.5-Pro。

### 方法 4：VGGT-QFormer 蒸馏（降低推理成本）

VGGT 1.2B 参数太重，推理吞吐量只有 4.39 samples/s。提出用 Q-Former 学生网络蒸馏 VGGT 的相机 token：
- 学生网络复用 Qwen2.5-VL 第 7 层的视觉特征（probing 显示此层相机信息最丰富）
- 交替使用局部注意力（每帧内）和全局注意力（跨帧），模仿 VGGT 的时序建模
- 三阶段渐进训练：先训分类器 → 再训蒸馏器 → 联合微调

结果：实例准确率从 73.8% 降到 63.8%（-10%），但吞吐量提升 **5.3×**，显存降低 **61%**。

### 局限性

1. 合成-真实域差距：数据全来自 Unreal Engine 渲染，真实视频上的泛化未充分验证
2. 只覆盖外参变化（平移/旋转），不包含内参变化（zoom）
3. 只用了 VGGT 一个 3D 基础模型，未探索其他选择
4. static 类别预测不稳定（VGGT 的重建先验假设相机在运动，静止场景是分布外样本）

---

## VGGT: Visual Geometry Grounded Transformer

**发表年份**：2025（arXiv 2503.11651，2025.03，牛津大学 VGG + Meta AI）

**代码**：https://github.com/facebookresearch/vggt

### 核心定位：统一的前馈 3D 重建基础模型

VGGT 是一个大型前馈 Transformer，给定 1 到数百张图像，**在一次前向传播中同时输出所有关键 3D 属性**：相机内外参、深度图、点云（point map）、3D 点轨迹（point tracks）。整个推理过程不到 1 秒，且通常优于需要后处理优化（Bundle Adjustment、Global Alignment）的传统方法。

**与 DUSt3R/MASt3R 的核心区别**：DUSt3R/MASt3R 每次只能处理两张图像，多图需要代价高昂的全局对齐后处理；VGGT 直接处理任意数量的图像，无需后处理即可使用。

---

### 模型架构

#### 输入与输出定义

**输入**：N 张 RGB 图像 $\{I_i\}_{i=1}^N$，以第一帧相机坐标系为世界坐标系。

**输出**（每帧一组）：

| 输出 | 符号 | 维度 | 说明 |
|------|------|------|------|
| 相机参数 | $g_i = [q, t, f]$ | 9 维 | 旋转四元数 $q \in \mathbb{R}^4$ + 平移向量 $t \in \mathbb{R}^3$ + 视场角 $f \in \mathbb{R}^2$ |
| 深度图 | $D_i$ | $H \times W$ | 每像素深度值 |
| 点云图 | $P_i$ | $3 \times H \times W$ | 每像素对应的 3D 坐标（在第一帧坐标系下） |
| 跟踪特征 | $T_i$ | $C \times H \times W$ | 用于点跟踪的稠密特征图 |

**坐标系约定**：第一帧相机坐标系为世界坐标系，第一帧的外参固定为单位矩阵（$q_1=[0,0,0,1]$，$t_1=[0,0,0]$）。

#### 主干网络：交替注意力（Alternating-Attention, AA）

```
输入图像 {I_1, ..., I_N}
    │
    └─ DINOv2 patchify → 每帧 K 个 token（tI_i）
    
    拼上：相机 token（tg_i）+ 4个寄存器 token（tR_i）
    ↓
    ┌─────────────────────────────────────────┐
    │  × L=24 层交替注意力块                   │
    │  ┌──────────────────────────────────┐   │
    │  │ Frame-wise Self-Attention        │   │
    │  │ （每帧内部的 token 互相注意）      │   │
    │  └──────────────────────────────────┘   │
    │  ┌──────────────────────────────────┐   │
    │  │ Global Self-Attention            │   │
    │  │ （所有帧的所有 token 互相注意）    │   │
    │  └──────────────────────────────────┘   │
    └─────────────────────────────────────────┘
    ↓
    输出 token：{t̂I_i, t̂g_i}（丢弃寄存器 token）
```

**为什么用交替注意力而非纯全局注意力**：消融实验表明，交替注意力（帧内 + 全局交替）优于纯全局自注意力和跨帧交叉注意力。纯全局注意力缺乏帧内归一化；跨帧交叉注意力虽然信息融合充分，但随帧数增加计算量爆炸，且实验上反而不如自注意力。

**第一帧的特殊处理**：第一帧的相机 token 和寄存器 token 使用一套独立的可学习参数（$\bar{t}^g, \bar{t}^R$），其他帧共用另一套（$t^g, t^R$），让模型能区分参考帧与其他帧。

#### 预测头

**相机头（Camera Head）**：
- 输入：输出相机 token $\{\hat{t}^g_i\}_{i=1}^N$
- 结构：4 层额外自注意力层 + 线性层
- 输出：相机内外参 $\hat{g}_i = [\hat{q}_i, \hat{t}_i, \hat{f}_i]$（9 维）

> **回答上一个问题**：相机 token 是 Transformer 内部的中间表示（维度 = 模型隐层维度，约 1024 维），**不直接等于外参矩阵**。外参是通过相机头（4 层自注意力 + 线性层）从相机 token 解码出来的 9 维向量。相机 token 比外参信息量更丰富，这也是为什么下游任务（如 Geometry-Guided 论文）直接用相机 token 而非解码后的外参效果更好。

**稠密预测头（DPT Head）**：
- 输入：输出图像 token $\{\hat{t}^I_i\}$
- 结构：DPT（Dense Prediction Transformer）上采样层，取第 4、11、17、23 层的 token 做多尺度融合，再用 3×3 卷积层映射
- 输出：深度图 $D_i$、点云图 $P_i$、跟踪特征 $T_i$，以及对应的不确定性图 $\Sigma^D_i, \Sigma^P_i$

**跟踪头（Tracking Head）**：
- 复用 CoTracker2 架构
- 输入：稠密跟踪特征 $T_i$ + 查询点 $y_q$
- 流程：在查询帧特征图上双线性采样查询点特征 → 与其他帧特征图做相关图 → 自注意力层 → 预测所有帧中的对应 2D 点
- 不假设帧的时序顺序，可处理任意无序图像集合

---

### 训练

#### 损失函数

$$\mathcal{L} = \mathcal{L}_{\text{camera}} + \mathcal{L}_{\text{depth}} + \mathcal{L}_{\text{pmap}} + 0.05 \cdot \mathcal{L}_{\text{track}}$$

- **相机损失**：Huber 损失，直接监督预测相机参数与 GT 的差距
- **深度损失**：不确定性加权损失 + 梯度项（$\Sigma^D \odot (\hat{D}-D) + \Sigma^D \odot (\nabla\hat{D}-\nabla D) - \alpha\log\Sigma^D$）
- **点云损失**：与深度损失类似，用点云不确定性加权
- **跟踪损失**：L1 损失 + 可见性二元交叉熵

跟踪损失权重 0.05 是因为其量级与其他损失不同，需要降权。

#### 坐标归一化

以所有 3D 点到第一帧相机的平均欧氏距离为尺度，归一化相机平移、点云和深度图。归一化**只作用于训练数据的 GT**，不作用于网络预测——让网络自己学会这种归一化，而非在推理时强制施加（实验表明强制归一化预测反而不稳定）。

#### 训练数据

16 个数据集，涵盖室内/室外/合成/真实场景：Co3Dv2、BlendMVS、DL3DV、MegaDepth、Kubric、WildRGB、ScanNet、HyperSim、Mapillary、Habitat、Replica、MVS-Synth、PointOdyssey、Virtual KITTI、Aria Synthetic Environments、Aria Digital Twin，以及一个类 Objaverse 的合成资产数据集。

#### 训练配置

- 模型规模：约 **1.2B 参数**（24 层 AA Transformer，隐层维度 1024，16 头，基于 ViT-L 规格）
- 每批随机采样 2~24 帧，批内总帧数固定为 48
- 图像分辨率：长边 resize 到 518，短边随机裁剪（168~518，14 的倍数）
- 优化器：AdamW，余弦学习率调度，峰值 lr=2e-4，warmup 8K 步，总 160K 步
- 硬件：64×A100，训练 9 天
- 使用 bfloat16 + 梯度检查点 + 梯度裁剪（阈值 1.0）

---

### 实验结果

#### 1. 相机位姿估计

**RealEstate10K + CO3Dv2**（每场景随机 10 帧，AUC@30 指标）：

| 方法 | Re10K AUC@30↑ | CO3Dv2 AUC@30↑ | 时间 |
|------|--------------|----------------|------|
| DUSt3R | 67.7 | 76.7 | ~7s |
| MASt3R | 76.4 | 81.8 | ~9s |
| VGGSfM v2 | 78.9 | 83.4 | ~10s |
| **VGGT（前馈）** | **85.3** | **88.2** | **~0.2s** |
| **VGGT + BA** | **93.5** | **91.8** | **~1.8s** |

VGGT 前馈模式（0.2s）超过所有需要后处理优化的方法（7~10s）。加 BA 后进一步提升，且因为 VGGT 直接输出高质量初始化，BA 无需三角化和迭代精化，只需约 1.6s 额外时间。

**IMC Challenge（phototourism 数据）**：

| 方法 | AUC@10↑ | 时间 |
|------|---------|------|
| VGGSfMv2（CVPR'24 冠军） | 76.82 | ~10s |
| **VGGT（前馈）** | 71.26 | **0.2s** |
| **VGGT + BA** | **84.91** | 1.8s |

VGGT + BA 超过 CVPR'24 IMC 挑战赛冠军。

#### 2. 多视图深度估计（DTU 数据集）

| 方法 | 是否需要 GT 相机 | Acc↓ | Comp↓ | Overall↓ |
|------|---------------|------|-------|---------|
| GeoMVSNet | ✓ | 0.331 | 0.259 | 0.295 |
| DUSt3R | ✗ | 2.677 | 0.805 | 1.741 |
| **VGGT** | **✗** | **0.389** | **0.374** | **0.382** |

VGGT 不需要 GT 相机，却达到了接近需要 GT 相机的方法的精度，远超 DUSt3R。

#### 3. 点云估计（ETH3D 数据集）

| 方法 | Acc↓ | Comp↓ | Overall↓ | 时间 |
|------|------|-------|---------|------|
| DUSt3R | 1.167 | 0.842 | 1.005 | ~7s |
| MASt3R | 0.968 | 0.684 | 0.826 | ~9s |
| **VGGT（点云头）** | 0.901 | 0.518 | 0.709 | **~0.2s** |
| **VGGT（深度+相机头）** | **0.873** | **0.482** | **0.677** | **~0.2s** |

**重要发现**：用深度头 + 相机头联合反投影得到的点云，比直接用点云头的精度更高。原因：把复杂任务（点云估计）分解为更简单的子任务（深度估计 + 相机估计），即使这些量在数学上是冗余的，联合监督也能提升精度。

#### 4. 图像匹配（ScanNet-1500）

| 方法 | AUC@5↑ | AUC@10↑ | AUC@20↑ |
|------|--------|---------|---------|
| Roma（SOTA 两视图匹配） | 31.8 | 53.4 | 70.9 |
| **VGGT** | **33.9** | **55.2** | **73.4** |

VGGT 的跟踪头并非专门为两视图匹配设计，却超过了专门的两视图匹配方法。

#### 5. 动态点跟踪（TAP-Vid Benchmark）

将 CoTracker2 的骨干网络替换为 VGGT 预训练特征，在 Kubric 上微调后：

| 方法 | Kinetics $\delta^{vis}_{avg}$↑ | RGB-S $\delta^{vis}_{avg}$↑ | DAVIS $\delta^{vis}_{avg}$↑ |
|------|-------------------------------|----------------------------|----------------------------|
| CoTracker2 | 49.6 | 67.4 | 61.8 |
| **CoTracker + VGGT** | **57.2** | **72.1** | **64.7** |

VGGT 预训练特征显著提升动态场景跟踪性能，尽管 VGGT 训练时只见过静态场景。

#### 6. 新视角合成（GSO 数据集）

微调 VGGT 做前馈新视角合成（不需要输入相机参数）：

| 方法 | 是否需要输入相机 | PSNR↑ | SSIM↑ | LPIPS↓ |
|------|--------------|-------|-------|--------|
| LVSM | ✓ | 31.71 | 0.957 | 0.027 |
| **VGGT-NVS** | **✗** | **30.41** | **0.949** | **0.033** |

不需要输入相机参数，仅用 20% 的训练数据，达到接近 LVSM 的性能。

---

### 消融实验关键结论

**注意力架构**（ETH3D Overall↓）：

| 架构 | Overall↓ |
|------|---------|
| 跨帧交叉注意力 | 1.061 |
| 纯全局自注意力 | 0.827 |
| **交替注意力（AA）** | **0.709** |

**多任务学习**（去掉某个损失后 ETH3D Overall↓）：

| 配置 | Overall↓ |
|------|---------|
| 无相机损失 | 0.834 |
| 无深度损失 | 0.727 |
| 无跟踪损失 | 0.790 |
| **全部损失** | **0.709** |

相机损失对点云精度提升最大（+0.125），说明相机估计和几何估计是强耦合的。

**图像 tokenization**：DINOv2 比从头训练的 14×14 卷积层效果更好，且训练更稳定（对超参数不敏感）。

---

### 推理速度与显存

| 帧数 | 时间（s） | 显存（GB） |
|------|---------|---------|
| 1 | 0.04 | 1.88 |
| 8 | 0.11 | 3.23 |
| 20 | 0.31 | 5.58 |
| 50 | 1.04 | 11.41 |
| 100 | 3.12 | 21.15 |
| 200 | 8.75 | 40.63 |

测试环境：单张 H100 GPU，图像分辨率 336×518，使用 Flash Attention v3。相机头约占总推理时间的 5%，每帧 DPT 头约 0.03s + 0.2GB。

---

### 局限性

1. **不支持鱼眼/全景相机**（假设针孔相机模型）
2. **极端旋转场景**重建质量下降
3. **大幅非刚性形变**场景失败（轻微动态可以处理）
4. **长视频显存爆炸**：全局注意力的显存随帧数平方增长，200 帧需要 40GB 显存

---

### 在本目录其他论文中的角色

| 论文 | VGGT 的用途 |
|------|-----------|
| **Geometry-Guided** | 用冻结 VGGT 的相机 token（2048 维中间特征）做相机运动分类；probing 实验的对比基线 |
| **VG-LLM** | 用冻结 VGGT 的编码器+融合解码器中间特征，与 2D 视觉特征相加，增强 MLLM 的 3D 感知 |
| **CA-MLLM** | 随 VG-LLM 基座带入，VGGT 冻结，不是 CA-MLLM 自身的架构贡献 |
| **ViPE** | 作为对比基线（ATE 8.4cm，~4min），ViPE 在精度和速度上均优于 VGGT |

---

## VG-LLM: Learning from Videos for 3D World

**发表年份**：2025（NeurIPS 2025）

### 核心问题：MLLM 缺乏跨帧 3D 几何感知

现有 MLLM 处理视频时，把每一帧独立编码为 token，**完全丢失了帧间的 3D 几何对应关系**（如同一个物体在不同帧的视差、深度变化）。这导致模型必须从纯语义 token 中"猜"3D 结构，既困难又需要大量监督。

以往解决方案（Video-3D LLM、GPT4Scene）依赖显式 3D 输入（深度图、点云、BEV 地图），在没有 3D 传感器的场景下无法使用。

**核心问题**：能否只用 RGB 视频，不依赖任何显式 3D 输入，让 MLLM 理解 3D 空间？

### 核心思路：外挂 3D 几何编码器，特征直接相加

VG-LLM 的思路极其简洁：在原有 2D 视觉编码器旁边，**并联一个 3D 视觉几何编码器**，两者的特征在 patch 级别直接相加后送入 LLM。

```
输入视频帧序列 {I_1, I_2, ..., I_n}
        │
        ├──→ 2D 视觉编码器（冻结）→ 语义 token T^V_i  ∈ R^{H/2p × W/2p × c}
        │
        └──→ 3D 几何编码器（冻结）→ 几何 token T^G_i  ∈ R^{H/p × W/p × c}
                                         ↓ 空间合并（2×2 concat + 2层MLP）
                                    T^G'_i ∈ R^{H/2p × W/2p × c}
                                         ↓
                    T^S_i = T^V'_i + T^G'_i  （逐元素相加）
                                         ↓
                              MLLM backbone（Qwen2.5-VL，解冻）
                                         ↓
                                      文本回答
```

### 模型架构详解

#### 2D 视觉编码器
使用 **Qwen2.5-VL** 内置的 ViT，对每帧独立提取语义特征。Qwen2.5-VL 会把相邻 2×2 patch 合并为一个 token（spatial merging），输出 $T^{V'}_i \in \mathbb{R}^{\lfloor h/2p \rfloor \times \lfloor w/2p \rfloor \times c}$。

#### 3D 视觉几何编码器
使用 **VGGT-1B**（Visual Geometry Grounded Transformer）的**编码器 + 融合解码器**部分（去掉任务头），对所有帧**联合**处理，输出每帧的几何感知特征 $T^G_i \in \mathbb{R}^{\lfloor h/p \rfloor \times \lfloor w/p \rfloor \times c}$。

VGGT 的关键能力：跨帧注意力机制，能捕捉帧间视差、对应关系，隐式编码了相机位姿变化和 3D 场景结构。

> **为什么用 VGGT 的特征而不是 VGGT 的预测输出（深度图、相机位姿）？**
> 消融实验表明：直接用 VGGT 预测的相机位姿/深度图/点云加到视觉 token 上，效果远不如直接用 VGGT 的**中间特征**。原因：预测输出是稀疏且有噪声的，而中间特征同时包含了所有这些信号，且更稠密、更鲁棒。

#### 特征融合
将 $T^G_i$ 做空间合并（与 Qwen2.5-VL 的 2×2 合并策略对齐）得到 $T^{G'}_i$，然后与 $T^{V'}_i$ **逐元素相加**：

$$T^S_i = T^{V'}_i + T^{G'}_i$$

消融实验对比了多种融合方式：
- Cross-Attention（1层/3层）：有提升但不如 Add
- Concat + MLP：在 ScanRefer 上反而比 baseline 差
- **Add（直接相加）**：最简单，效果最好

#### 训练策略
- **冻结**：2D 视觉编码器（Qwen2.5-VL 的 ViT）、3D 几何编码器（VGGT）、多模态连接器
- **解冻**：MLLM backbone（Qwen2.5-VL 的语言模型部分）
- 训练 1 epoch，Adam 优化器，batch size 64，学习率 1e-5
- 硬件：8×H100 80G，3D 场景理解任务约 9-12 小时

### 支持的任务

VG-LLM 用统一的**文本生成**目标处理所有 3D 任务（无需任务特定头）：

| 任务 | 输入 | 输出格式 |
|------|------|---------|
| **3D 视觉定位**（ScanRefer） | 视频帧 + 文本描述 | `{"frame": 12, "bbox_3d": [x,y,z,w,h,d,ψ,θ,φ]}` |
| **3D 稠密描述**（Scan2Cap） | 视频帧 + 3D 坐标 | 自然语言描述 |
| **3D 视频目标检测**（EmbodiedScan） | 视频帧 | JSON 列表，每项含类别 + 3D 框 |
| **空间推理**（VSI-Bench） | 视频帧 + 问题 | 文本回答 |

坐标系：以**第一帧相机坐标系**为世界坐标系（与 VGGT 一致）。

### 实验结果

**3D 场景理解**（无需任何显式 3D 输入）：
- ScanRefer Acc@0.25：VG-LLM-8B 达到 41.6%（+9.7% vs. SPAR），加 proposal refinement 后 57.6%，接近 Video-3D LLM（58.1%，需要 3D 输入）
- Scan2Cap CIDEr@0.5：80.0，与 Video-3D LLM 持平，无需 3D 输入
- 3D 视频目标检测 F1：+8.2%/+8.7%（4B/8B），3D 几何显著提升跨帧检测

**空间推理**：
- VSI-Bench：VG-LLM-4B 47.3%，超过 Gemini-1.5-Pro（45.4%）；VG-LLM-8B 50.7%，新 SOTA
- CV-Bench 3D：VG-LLM-8B 91.1%，最高

### 局限性

1. **跨相机泛化差**：VG-LLM 同样忽略了相机内参，在不同焦距相机上性能崩溃（这正是 CA-MLLM 要解决的问题）
2. **VGGT 内存随帧数爆炸**：长视频处理受限
3. **训练和推理都需要 VGGT**：推理时 VGGT 不可省略，增加计算开销

---

## On the Generalization Capacities of MLLMs for Spatial Intelligence

**发表年份**：2026（ICLR 2026）

### 核心问题：RGB-only 的空间推理 MLLM 无法跨相机泛化

现有"直接输入 RGB 做 3D 定位"的 MLLM 存在一个根本性缺陷：**忽略了相机内参**。这导致模型把物体的物理属性和相机的透视属性混在一起，无法区分：

- **焦距-深度歧义**：焦距变大（zoom in）和物体靠近，在图像上完全一样
- **尺寸-深度歧义**：小物体近处 vs. 大物体远处，投影尺寸可以相同

**实验证据**：
- 在 ScanNet 上训练好的模型，测试时把图像 resize 0.8×，F1 从 45.7% 直接崩到 24.3%（-21%）
- 混合多个数据集（ScanNet + ARKitScenes + Matterport3D 等）训练，性能反而比单数据集下降——因为不同数据集的相机焦距分布不同，模型被互相矛盾的几何信号搞混了

**理论分析**：图像 resize 因子 s 等价于相机内参缩放 $(f_x, f_y, c_x, c_y) \to (sf_x, sf_y, sc_x, sc_y)$，模型会把深度估计错误地缩放 $1/s$ 倍，导致系统性的定位偏移。

### 核心框架：Camera-Aware MLLM（CA-MLLM）

三个核心组件，统一集成在**几何感知视觉编码器(GAVE)**中：

**① 稠密相机射线嵌入（Dense Camera Ray Embedding）**

对每个视觉 token（位置 $(i,j)$），根据相机内参 $(f_x, f_y, c_x, c_y)$ 计算其对应的视线方向：

$$R_x[i,j] = \frac{u_{ij} - c_x}{f_x}, \quad R_y[i,j] = \frac{v_{ij} - c_y}{f_y}$$

再加上全局焦距值 $f_x, f_y$，用正弦位置编码生成稠密相机嵌入 $E_{cam}$，与视觉特征 $F_{vis}$ 逐元素相加。效果：每个 token 不再只是语义描述符，还知道自己对应 3D 空间中的哪条射线。

**② 相机感知几何增强（Camera-Aware Geometric Augmentation）**

训练时随机扰动相机内参：
- **缩放**：图像 resize 因子 s，同步更新内参 $(f_x, f_y, c_x, c_y) \to (sf_x, sf_y, sc_x, sc_y)$
- **平移**：随机偏移主点 $(c_x, c_y)$，模拟非中心投影

关键：图像和内参**同步更新**，保证几何正确性。强迫模型学会把场景内容和相机几何解耦，而不是记住训练相机的特定参数。

**③ 几何先验蒸馏（Geometric Prior Distillation）**

**动机**：直接的 3D 标注数据稀缺，但大规模 RGB-深度对数据丰富。论文利用这一资源，从预训练的单目度量深度估计（MMDE）模型中蒸馏几何先验，而不是从头学习 3D 结构。

**具体做法**：

用冻结的 **UniDepth v2**（在 1000 万 RGB-深度对上预训练）对每张训练图像做前向推理，输出一张稠密 3D 点云（每个像素对应一个 $(X, Y, Z)$ 坐标）。将这个点云编码为几何先验嵌入 $E_{geo} \in \mathbb{R}^{H \times W \times D}$，与视觉特征 $F_{vis}$ 逐元素相加：

$$F_{vis}' = F_{vis} + E_{cam} + E_{geo}$$

这样每个视觉 token 同时携带了：语义外观信息（$F_{vis}$）、相机射线方向（$E_{cam}$）、以及该像素对应的 3D 空间位置（$E_{geo}$）。

**为什么叫蒸馏**：UniDepth v2 是教师模型，它的知识（对每个像素的 3D 理解）被蒸馏进 MLLM 的视觉 token 中。MLLM 自身不需要直接预测深度，而是通过 $E_{geo}$ 间接获得了 UniDepth 的几何感知能力。训练时 UniDepth 权重完全冻结，只有 MLLM 的参数在更新。

**无内参时的 Fallback**：UniDepth v2 本身可以从单张 RGB 图像直接估计相机内参，因此当数据没有内参标注时（无论训练还是推理），可以用 UniDepth 估计的内参来计算 $E_{cam}$。论文原文（第7页）明确写道：*"geometric prior distillation extends the framework to images with unknown camera parameters, as UniDepth is able to estimate intrinsics directly from images. This enables training **and evaluation** on vast 2D datasets lacking such camera intrinsics annotations."* 这使得大量没有相机参数标注的互联网图像也可以纳入训练和推理。

### 实验结果

**跨相机泛化（核心验证）**：

在 ScanNet 上训练，测试时对图像做不同比例的 resize（模拟不同焦距的相机）：

| 方法 | 原始图像 F1 | resize×0.8 F1 | resize×1.2 F1 |
|------|-----------|--------------|--------------|
| Qwen2.5-VL（基线） | 45.7% | 24.3% | 31.6% |
| VG-LLM（基线） | 46.0% | 25.8% | 33.2% |
| **CA-MLLM（ours）** | **竞争性** | **大幅保持** | **大幅保持** |

基线在 resize 后性能崩溃，CA-MLLM 保持稳健。

**标准空间推理 Benchmark**：

- **SPAR-Bench**：CA-MLLM-4B 平均分 81.23，超过 SPAR-8B（53.50）和 VG-LLM-4B（71.99），甚至超过 GPT-5（37.40）和 Gemini-2.5-Pro（36.30）
- **VSI-Bench**：CA-MLLM-4B 达到 46.8，与 VG-LLM-4B（47.3）相当，优于同规模开源模型

### 消融实验结论

单独使用相机感知架构（射线嵌入）或单独使用几何增强，都只有中等提升；**两者结合才能解锁显著的跨相机泛化能力**——架构层面的相机感知 + 数据层面的多样性缺一不可。

### 与其他论文的关系

| 论文 | 思路 |
|------|------|
| CameraMotionDataset | 外挂 3D 基础模型提取相机运动线索，prompt 注入，不改 VLM |
| CA-MLLM（本篇） | 把相机内参直接编码进每个视觉 token，从架构层面解决歧义 |

两篇都强调"相机参数对空间理解至关重要"，但解决路径不同：前者是推理时注入，后者是训练时内化。

---

## VG-LLM vs. CA-MLLM：深度对比分析

CA-MLLM 明确以 VG-LLM 为基线，在其基础上解决跨相机泛化问题。两者的关系是**递进式改进**：VG-LLM 解决了"如何从 RGB 视频中获取 3D 几何感知"，CA-MLLM 解决了"如何让这种感知跨相机泛化"。

### 模型架构对比

| 维度 | VG-LLM | CA-MLLM |
|------|--------|---------|
| **基座 LLM** | Qwen2.5-VL（3B/7B） | Qwen2.5-VL（3B/7B） |
| **2D 视觉编码器** | Qwen2.5-VL 内置 ViT（冻结） | Qwen2.5-VL 内置 ViT（冻结） |
| **3D 几何来源** | VGGT-1B 中间特征（冻结） | UniDepth v2 点云（冻结）；VGGT 随基座带入但不是 CA-MLLM 的架构贡献 |
| **相机内参处理** | ❌ 完全忽略 | ✅ 每个 token 注入射线方向 + 焦距 |
| **几何特征注入方式** | $T^S = T^V + T^G$（VGGT 特征直接加） | $F' = F_{vis} + E_{cam} + E_{geo}$（射线嵌入 + 点云嵌入） |
| **数据增强** | 无相机感知增强 | 随机缩放/平移内参，图像同步变换 |
| **推理时是否需要 3D 模型** | ✅ 需要 VGGT（推理时必须运行） | ✅ 需要 UniDepth v2（CA-MLLM 自己的架构组件）；VGGT 随基座带入 |
| **无内参时的处理** | 无（直接忽略） | UniDepth 估计内参作为 fallback |

### Forward 流程对比

#### VG-LLM 的 Forward 流程

```
输入：视频帧序列 {I_1, ..., I_n} + 文本问题 Q

Step 1: 2D 语义特征提取（逐帧独立）
  每帧 I_i → Qwen2.5-VL ViT → T^V_i ∈ R^{H/p × W/p × c}
  → 2×2 spatial merging → T^V'_i ∈ R^{H/2p × W/2p × c}

Step 2: 3D 几何特征提取（所有帧联合）
  {I_1, ..., I_n} → VGGT encoder + fusion decoder（跨帧注意力）
  → {T^G_1, ..., T^G_n}，每帧 T^G_i ∈ R^{H/p × W/p × c}
  → 2×2 concat + 2层MLP → T^G'_i ∈ R^{H/2p × W/2p × c}

Step 3: 特征融合
  T^S_i = T^V'_i + T^G'_i  （逐元素相加）

Step 4: LLM 推理
  {T^S_1, ..., T^S_n} + text_embed(Q) → Qwen2.5-VL LLM → 文本回答
```

**关键特点**：VGGT 在推理时必须运行，计算开销不可省略。

#### CA-MLLM 的 Forward 流程

CA-MLLM **论文自己定义的架构**（图4）只包含以下组件，VGGT 不在其中：

```
输入：图像/视频帧 I + 相机内参（可选）+ 文本问题 Q

Step 1: 2D 语义特征提取
  I → 2D Visual Encoder（ViT，冻结）→ F_vis ∈ R^{H×W×D}

Step 2: 相机射线嵌入（训练+推理都需要）
  若有内参：直接计算
    R_x[i,j] = (u_ij - c_x) / f_x
    R_y[i,j] = (v_ij - c_y) / f_y
    + 全局焦距 f_x, f_y
  若无内参：用冻结的 UniDepth v2 先估计内参，再计算
  → 正弦位置编码 → E_cam ∈ R^{H×W×D}

Step 3: 几何先验嵌入（训练+推理都需要）
  I → 冻结的 MMDE（UniDepth v2）→ 稠密 3D 点云
  → 3D Geometric Prior Distillator（可训练）→ E_geo ∈ R^{H×W×D}

Step 4: 特征融合（GAVE）
  F' = F_vis + E_cam + E_geo

Step 5: LLM 推理
  F' + text_embed(Q) → Camera-Aware MLLM（可训练）→ 文本回答
```

**关于 VGGT 的说明**：

VGGT 只出现在附录B的实现细节里：
> "Our work **builds upon the VG-LLM-4B** framework... The VG-LLM-4B model integrates Qwen2.5-VL-3B with **VGGT-1B** for 3D geometry feature extraction. **To further enhance** the model's geometric understanding ability, we employ **UniDepth v2**..."

CA-MLLM 选用 VG-LLM-4B 作为实现基座，VGGT 随之带入，但**CA-MLLM 论文自己的架构贡献（图4、第4节）完全不包含 VGGT**。从论文的视角，VGGT 属于基座内部的实现细节，不是 CA-MLLM 框架的组成部分。

**可训练部分**：MLLM backbone、Camera Ray Embedding 编码层、3D Geometric Prior Distillator

**冻结部分**：2D Visual Encoder（ViT）、UniDepth v2（以及基座内的 VGGT）
### 核心设计哲学差异

| 问题 | VG-LLM 的答案 | CA-MLLM 的答案 |
|------|--------------|---------------|
| **3D 几何从哪来** | 从 VGGT 的跨帧注意力特征中隐式获取 | 从 UniDepth 的点云中显式蒸馏，再从内参中显式计算射线 |
| **相机内参的角色** | 忽略（假设所有相机一样） | 核心输入，每个 token 都感知自己的视线方向 |
| **推理时的代价** | 高（VGGT 1.2B 参数必须运行） | 高（UniDepth v2 + 基座内的 VGGT 均需运行） |
| **跨相机泛化** | 差（内参不同时性能崩溃） | 好（内参感知 + 数据增强共同保证） |
| **无内参数据的处理** | 无法处理（直接忽略内参） | 用 UniDepth 估计内参，无缝处理 |
| **几何信息的形式** | 隐式（VGGT 特征中混合了几何和语义） | 显式（射线方向 = 纯几何；点云 = 纯 3D 坐标） |

### 实验数字对比

在 CA-MLLM 论文的跨相机泛化实验中（ScanNet 训练，resize 测试）：

| 模型 | 原始 F1 | resize×0.8 F1 | resize×1.2 F1 |
|------|---------|--------------|--------------|
| Qwen2.5-VL-3B | 45.7% | 24.3%（**-47%**） | 31.6%（**-31%**） |
| VG-LLM-4B | 46.5% | 25.8%（**-44%**） | 33.2%（**-29%**） |
| **CA-MLLM-4B** | 竞争性 | **大幅保持** | **大幅保持** |

VG-LLM 在跨相机测试上与 Qwen2.5-VL 一样脆弱——因为 VGGT 的特征虽然包含 3D 信息，但同样没有内参感知，无法解决焦距-深度歧义。

### 一句话总结

> **VG-LLM**：用 VGGT 的跨帧几何特征增强 MLLM，解决了"没有 3D 输入也能做 3D 理解"的问题，但仍然相机无关。
>
> **CA-MLLM**：在 VG-LLM（VGGT）的基础上，额外引入 UniDepth v2 蒸馏点云几何先验，并把相机内参显式编码进每个 token，配合相机感知数据增强，解决了"跨相机泛化"的根本问题。推理时 VGGT 和 UniDepth v2 均需运行（均冻结），可训练的是 MLLM backbone、射线嵌入层和几何先验蒸馏器。

---

## ViPE: Video Pose Engine for 3D Geometric Perception

**发表年份**：2025（arXiv 2025.08，NVIDIA）

### 核心定位：大规模视频标注引擎

ViPE 不是一篇"理解相机运动语义"的论文，而是一个**工程系统**——从任意野外视频中高效、准确地估计相机内参、相机位姿轨迹和稠密度量深度图，用于大规模数据标注。已被用于 NVIDIA 的 Gen3C（世界生成）和 Cosmos（视频扩散模型）的训练数据标注。

### 核心问题：现有方法各有短板

| 方法类型 | 代表 | 问题 |
|---------|------|------|
| 经典 SLAM/SfM | COLMAP、ORB-SLAM | 假设静态场景、需要已知内参、动态物体鲁棒性差 |
| 端到端前馈模型 | DUSt3R、MASt3R、VGGT | 内存随帧数爆炸，长视频不可处理 |
| 混合系统 | MASt3R-SLAM、VGGT-SLAM | 前端和后端松耦合，精度和鲁棒性不足 |

ViPE 的目标：兼顾经典 SLAM 的可扩展性/精度 + 学习方法的鲁棒性，同时支持动态场景和多种相机模型。最近邻工作是 MegaSAM，ViPE 相比它：不需要逐帧优化（更快）、更好地处理动态物体、支持更多相机模型、精度更高。

### 系统架构：六步流水线

```
输入视频
  │
  ├─ Step 1: 内参初始化（均匀采4帧 → GeoCalib）
  │
  ├─ Step 2: 关键帧选择（稠密光流 + 稀疏点运动量 > 阈值 → 关键帧）
  │
  ├─ Step 3: 前端跟踪（滑动窗口局部BA，Gauss-Newton求解）
  │
  ├─ Step 4: 后端全局BA（关键帧数达8/16/64时触发，解锁内参联合优化，同步更新深度先验）
  │
  ├─ Step 5: 非关键帧位姿填充（连接最近2个关键帧，全部并行处理）
  │
  └─ Step 6: 稠密深度对齐（Video Depth Anything相对深度 + BA绝对尺度 → 动量仿射对齐）
       │
       └─ 输出：每帧位姿 T_i + 内参 k + 稠密度量深度图 D_i
```

**关键设计**：非关键帧只连接到最近两个关键帧，且只加单向边（关键帧→非关键帧），不需要为非关键帧计算深度，大幅降低计算量，并行处理所有非关键帧。

### 核心能量函数：三项约束联合优化

$$e_{\text{ViPE}}(\{T_i\}, \{D_i\}, k) = \sum_{(i,j)\in\mathcal{E}} e_{\text{dense}} + \sum_{(i,j)\in\mathcal{E}} e_{\text{sparse}} + \alpha \sum_{i\in\mathcal{V}} e_{\text{depth}}$$

**未知变量**：帧位姿 $T_i \in SE(3)$、每关键帧低分辨率深度图 $D_i \in \mathbb{R}^{h \times w}$（$h=H/8, w=W/8$）、相机内参 $k$。

#### ① 稠密光流约束 $e_{\text{dense}}$

$$e_{\text{dense}} = \sum_u w[u] \cdot \left\| \Pi_k(T_j^{-1} T_i \circ \Pi_k^{-1}(D_i[u])) - u - F_{ij}[u] \right\|^2$$

**直觉**：把帧 $i$ 中像素 $u$ 对应的 3D 点，用当前估计的位姿投影到帧 $j$，看投影位置与光流预测的位置是否一致。

- $\Pi_k^{-1}(D_i[u])$：把像素 $u$ 反投影为 3D 点（需要深度和内参）
- $T_j^{-1} T_i$：把 3D 点从帧 $i$ 坐标系变换到帧 $j$ 坐标系
- $\Pi_k(\cdot)$：再投影到帧 $j$ 的图像平面
- $F_{ij}[u]$：光流网络（来自 DROID-SLAM）预测的像素位移，内部用代价体积 + 迭代精化，以当前位姿估计的投影流 $F^{\text{prior}}_{ij}$ 作为初始引导
- $w[u]$：置信度权重，同时编码了动态物体掩码（动态区域权重为 0）

#### ② 稀疏特征点约束 $e_{\text{sparse}}$

$$e_{\text{sparse}} = \sum_{p_i} \left\| \Pi_k(T_j^{-1} T_i \circ \Pi_k^{-1}(\text{Bilerp}(D_i, p_i))) - p_j \right\|^2$$

**为什么需要**：稠密光流分辨率低（$H/8 \times W/8$），会丢失高分辨率细节。稀疏特征点在**原始高分辨率图像**上计算，提供亚像素精度约束。

**特征提取**：NVIDIA cuVSLAM 包中的 CUDA 加速模块，Shi-Tomasi 角点检测 + Lucas-Kanade 光流跟踪。

**工程细节**：直接用双线性插值会导致 Hessian 矩阵变稠密（一个约束涉及多个深度像素），改用**双线性 splatting**，把稀疏约束转化为与稠密约束相同的形式，保持 Hessian 的稀疏性，求解更高效。

#### ③ 深度正则化 $e_{\text{depth}}$

$$e_{\text{depth}}(D_i) = \sum_u m[u] \cdot \left\| D_i[u] - D^{\text{prior}}_i[u] \right\|^2$$

用预训练单目度量深度估计模型提供深度先验，防止小运动/退化运动时的尺度模糊，同时提供真实世界度量尺度（解决 SLAM 系统常见的尺度漂移）。

**可选深度网络**：Metric3Dv2（默认）、UniDepthV2、UniK3D（支持更多相机模型）。

**关键细节**：这些深度网络的预测以内参为条件，因此每次后端优化更新内参后，都要**重新预测深度先验**，替换 $D^{\text{prior}}_i$。

### 动态物体处理

用 GroundingDINO + SAM + XMem 的组合：
- 每隔固定帧数检测可移动物体（人、车等）的分割掩码
- 用 XMem 在帧间传播掩码（避免逐帧推理的高计算成本）
- 稠密约束：$w[u] = M[u] \times \text{光流置信度}$（动态区域权重为 0）
- 稀疏约束：直接删除落在动态区域内的特征点

### 多相机模型支持

用统一的径向相机模型覆盖所有相机类型：

$$u = \Pi_k\left(\begin{bmatrix}x\\y\\z\end{bmatrix}\right) = \begin{bmatrix} f \cdot q_k(\theta) \cdot \cos\varphi + W/2 \\ f \cdot q_k(\theta) \cdot \sin\varphi + H/2 \end{bmatrix}$$

其中 $\theta = \arctan\frac{\sqrt{x^2+y^2}}{z}$（光线与光轴夹角），$\varphi = \arctan\frac{y}{x}$（方位角）。

| 相机类型 | $q_k(\theta)$ | 参数 $k$ |
|---------|--------------|---------|
| 针孔 | $\tan\theta$ | $f$（1个参数）|
| 广角/鱼眼 | $\frac{\tan\theta}{1+\alpha\sqrt{\tan^2\theta+1}}$ | $[f, \alpha]$（2个参数）|
| 360°全景 | 投影为6个针孔相机 | 固定相对变换 |

360° 全景处理：把全景图投影为朝向前/后/左/右/上/下的 6 个针孔相机视图，固定它们之间的相对变换 $T_v$，然后用多相机 BA 处理。

### 深度后处理：平滑深度对齐

BA 优化的深度图 $D^{\text{BA}}_i$：跨帧一致、有绝对尺度，但分辨率低（$H/8$）、纹理缺失区域噪声大。视频深度网络（Video Depth Anything）的深度图 $D^{\text{VDA}}_i$：高质量、时序平滑，但只有相对深度（无绝对尺度）。

**解决方案**：用 BA 深度的绝对尺度来校准视频深度的高质量相对结构。对每帧求最优仿射变换参数（在逆深度空间）：

$$\alpha_i, \beta_i = \arg\min_{\alpha,\beta} \sum_{u \text{ valid}} \left\| M \cdot \left(\frac{\alpha}{D^{\text{VDA}}_i[u]} + \beta - \frac{1}{D^{\text{BA}}_i[u]}\right) \right\|^2_2$$

**动量更新**防止帧间跳变：$\hat{\alpha}_i = m \cdot \hat{\alpha}_{i-1} + (1-m) \cdot \alpha_i$

最终输出：$D^{\text{HD}}_i = \frac{1}{\hat{\alpha}_i / D^{\text{VDA}}_i + \hat{\beta}_i}$（高分辨率、时序一致、度量尺度）

**极端情况**：BA 深度覆盖率低时用 PriorDA 填充；几乎没有 BA 深度时直接用 §3.2.3 的度量深度估计。

### 推理速度与子模型

**速度**：3~5 FPS（640×480 分辨率，单张 RTX 5090 GPU），不含逐帧稠密深度估计时间。

| 方法 | 运行时间（~950帧序列） |
|-----|---------------------|
| MegaSAM | ~15 min |
| VGGT | ~4 min |
| **ViPE** | **~3 min** |
| DROID-SLAM | ~2 min |
| MASt3R-SLAM | ~1.5 min |

**推理时需要同时运行的子模型**：

| 子模型 | 用途 | 是否必须 |
|-------|------|---------|
| GeoCalib | 内参初始化 | 是 |
| DROID-SLAM 光流网络 | 稠密光流约束 | 是 |
| cuVSLAM (CUDA) | 稀疏特征跟踪 | 是 |
| Metric3Dv2 / UniDepthV2 | 深度先验正则化 | 是 |
| GroundingDINO + SAM + XMem | 动态物体掩码 | 可选（有动态物体时建议开） |
| Video Depth Anything | 最终稠密深度对齐 | 是（需要高质量深度图时） |

### 实验结果

**相机位姿估计**（TUM RGB-D，室内场景）：

| 方法 | ATE↓ (静态) | ATE↓ (动态) | 焦距误差↓ | 运行时间 |
|-----|-----------|-----------|---------|--------|
| DROID-SLAM | 4.4 cm | 2.7 cm | 4.1° | ~2 min |
| MASt3R-SLAM | 6.8 cm | 7.6 cm | N/A | ~1.5 min |
| VGGT | 8.4 cm | 12.9 cm | 11.1° | ~4 min |
| MegaSAM | 7.0 cm | 1.5 cm | 10.5° | ~15 min |
| **ViPE** | **3.6 cm** | **1.5 cm** | **1.8°** | **~3 min** |

ViPE 在静态场景上比 DROID-SLAM 还好，动态场景与 MegaSAM 持平，但**焦距估计精度远超所有方法**（1.8° vs 10.5°+）。

**相机位姿估计**（KITTI 室外驾驶场景）：ViPE ATE 9.2m，优于 MegaSAM（25.4m）约 **63%**，输出轨迹接近真实世界度量尺度（MegaSAM 输出尺度不确定）。

**深度估计**（SINTEL + ETH3D）：

| 方法 | SINTEL RelAbs↓ | ETH3D RelAbs↓ | ETH3D δ1.25↑ |
|-----|--------------|--------------|-------------|
| UniDepth | 0.23 | 0.19 | 72.1% |
| VGGT | 0.22 | 0.20 | 69.9% |
| MegaSAM | 0.29 | 0.23 | 64.9% |
| **ViPE** | **0.21** | **0.16** | **81.7%** |

ViPE 深度估计最优，且帧间时序一致性远好于逐帧方法（无抖动）。

**消融实验**：

| 组件 | S-ATE↓ | S-Focal↓ | Sampson↓ |
|-----|--------|---------|---------|
| 仅稠密约束 | 1.39 | 5.28 | 1.40 |
| +稀疏约束 | 1.40 | 5.20 | 1.36 |
| +深度正则化 | 1.35 | 5.00 | 0.84 |
| +动态掩码 | 1.13 | 4.45 | 0.96 |
| **全部组件** | **1.05** | **4.26** | **0.83** |

动态掩码对位姿精度提升最大，深度正则化对几何一致性（Sampson 误差）提升最大。

**无 GT 视频评测**：提出两个新指标——Shuttle Pose Error（正向/反向视频位姿一致性）和 Sampson Error（极线几何误差），在真实野外视频上均优于 MegaSAM 和 VGGT。

### 数据集发布（共 ~9600 万帧）

| 数据集 | 规模 | 内容 |
|--------|------|------|
| **Dynpose-100K++** | 99,501 视频，15.7M 帧 | 真实互联网视频（重新标注，原 Dynpose-100K 质量不足，仅 12FPS 且无逐帧几何） |
| **Wild-SDG-1M** | ~100 万视频，78M 帧 | AI 生成视频（视频扩散模型，文本提示均衡采样） |
| **Web360** | ~2,000 视频 | 360° 全景视频（ERP 格式，含游戏场景，提供逐帧位姿和距离图） |

### 与本目录其他论文的关系

ViPE 解决的是**几何估计**问题（相机位姿 + 深度），而非相机运动的**语义理解**（pan/tilt/dolly 等）。它是 CameraBench、CamReasoner 等论文中 SfM/SLAM 基线方法的"升级版"，代表了几何方法的当前最优水平，但仍无法理解语义层面的相机运动意图。

**适合使用场景**：
- ✅ 批量标注训练数据（ViPE 的主要设计目标，已用于 Gen3C、Cosmos）
- ✅ 离线处理野外视频（手持、自拍、电影镜头、行车记录仪、360°全景均支持）
- ✅ 需要度量尺度的位姿和深度（不是相对尺度）
- ❌ 实时推理（3~5 FPS 不够实时）
- ❌ 嵌入到另一个模型的推理链路（多子模型组合，不适合端到端集成）

---

## DROID-W: DROID-SLAM in the Wild

**发表年份**：2026（arXiv 2603.19076，2026.03，ETH Zurich + Microsoft）

**代码**：https://github.com/MoyangLi00/DROID-W

### 核心问题

传统 SLAM（包括 DROID-SLAM）假设场景静态，动态物体（行人、车辆、动物等）会破坏刚体运动假设，导致：
- 重投影残差不可靠 → BA 优化发散
- 相机轨迹漂移或跟踪失败

现有动态 SLAM 方案的局限：
- **基于分割/检测的方法**（DynaSLAM、DS-SLAM）：依赖预定义动态类别先验，遇到未知动态物体失效
- **基于 NeRF/GS 的不确定性方法**（WildGS-SLAM、UP-SLAM）：不确定性优化依赖高质量静态场景重建，在杂乱真实场景中重建质量差 → 不确定性估计错误 → 恶性循环

**核心思路**：用**多视图视觉特征一致性**（而非重投影残差）来估计逐像素动态不确定性，不依赖场景重建质量，也不依赖动态类别先验。

---

### 方法：不确定性感知可微 Bundle Adjustment

#### 背景：DROID-SLAM 的 BA 框架

DROID-SLAM 维护两个状态变量：
- 相机位姿 $G_t \in SE(3)$
- 逆深度 $d_t \in \mathbb{R}^{H/8 \times W/8}$

通过可微 BA 最小化稠密重投影残差：

$$E(G', d') = \sum_{(i,j)\in E} \|p^*_{ij} - p_{ij}\|^2_{\Sigma_{ij}}, \quad \Sigma_{ij} = \text{diag}(w_{ij})$$

其中 $w_{ij}$ 是 DROID-SLAM 预测的置信度图，$p^*_{ij}$ 是预测的稠密对应点。

#### 核心改进 1：不确定性感知 BA（UBA）

引入逐像素动态不确定性 $u_t \in \mathbb{R}^{H/8 \times W/8}$，修改 Mahalanobis 距离权重：

$$\Sigma^{\text{uncer}}_{ij} = \text{diag}\left(w_{ij} \cdot \frac{1}{u'_i}\right)$$

不确定性高的像素（动态区域）权重降低，减少其对 BA 的干扰。

**交替优化策略**（避免联合优化 Hessian 矩阵过大）：
1. **位姿-深度精化**：固定不确定性，最小化 $\hat{E}(G', d') = \sum_{(i,j)\in E} \|p^*_{ij} - p_{ij}\|^2_{\Sigma^{\text{uncer}}_{ij}}$
2. **不确定性优化**：固定位姿-深度，更新不确定性（见下）

#### 核心改进 2：基于多视图特征相似度的不确定性优化

**关键设计**：用 DINOv2 特征的余弦相似度衡量多视图一致性，而非重投影残差。

原因：重投影残差在大幅动态运动下不可靠；DINOv2 特征对外观变化鲁棒，语义一致性更稳定。

具体流程：
1. 用 **FiT3D**（精调版 DINOv2）提取每帧稠密特征 $F_i$
2. 对图像对 $(I_i, I_j)$，用当前位姿-深度估计计算刚体运动对应点 $p_{ij}$
3. 在 $F_j$ 上双线性插值得到对应特征 $F_{ij}$
4. 计算余弦相似度，构造相似度损失：

$$E_{\text{sim}}(u') = \sum_{(i,j)\in E} \frac{1 - \frac{F_i \cdot F_{ij}}{\|F_i\|_2 \|F_{ij}\|_2}}{u'_i \cdot u'_{ij}}$$

动态区域特征不一致（相似度低）→ 分子大 → 需要大的 $u'$ 来降低损失 → 不确定性自动升高。

**双向不确定性解耦**：对每个图像对分别优化 $u'_i$ 和 $u'_{ij}$，避免帧间动态相互干扰。

**防止平凡解**（$u' \to +\infty$）：加对数先验正则化：

$$E_{\text{prior}}(u') = \sum_i \log(u'_i + 1.0)$$

**总不确定性损失**：

$$E_{\text{uncer}}(u') = E_{\text{sim}}(u') + \gamma_{\text{prior}} E_{\text{prior}}(u')$$

#### 核心改进 3：仿射映射正则化

直接优化逐像素不确定性会导致空间不一致和过拟合。改为学习一个局部仿射映射：

$$u = \text{Softplus}(\theta \cdot F)$$

其中 $\theta$ 是可学习参数，$F$ 是 DINOv2 特征。用梯度下降（带 weight decay）优化 $\theta$，所有反向传播用 CUDA 实现以保证效率。

#### 核心改进 4：单目深度正则化

DROID-SLAM 将逆深度初始化为常数 1，在高动态场景中导致初始化不准。引入 **Metric3D** 预测的度量单目深度 $D_t$ 作为 BA 的深度正则化：

$$E^+(G', d') = \sum_{(i,j)\in E} \|p^*_{ij} - p_{ij}\|^2_{\Sigma^{\text{uncer}}_{ij}} + \gamma_d \sum_i \|d'_i - D_i\|^2$$

---

### 系统流程

```
输入 RGB 视频序列
    │
    ├─ DROID 特征提取器 → 稠密对应点 p*_ij + 置信度 w_ij
    ├─ FiT3D（精调 DINOv2）→ 语义特征 F_i
    └─ Metric3D → 度量单目深度 D_t
    
    ↓ 累积 12 个关键帧初始化
    
    ┌─────────────────────────────────────────────────┐
    │  交替迭代优化                                     │
    │  ① 不确定性感知 BA（位姿 + 深度精化）             │
    │  ② 不确定性优化（仿射映射参数 θ 更新）            │
    └─────────────────────────────────────────────────┘
    
    ↓ 前端跟踪完成后
    
    全局 BA（冻结不确定性参数，精化所有关键帧位姿和深度）
    
    输出：相机轨迹 + 动态点云 + 逐像素不确定性图
```

**注意**：全局 BA 阶段冻结不确定性参数，因为仿射映射设计用于局部滑动窗口内的正则化，不适合全局尺度。

---

### 实验结果

#### 1. Bonn RGB-D Dynamic 数据集（ATE RMSE ↓ [cm]）

| 方法 | 平均 ATE |
|------|---------|
| DROID-SLAM | 4.91 |
| DynaMoN (MS&SS) | 4.10 |
| DynaMoN (MS) | 4.02 |
| WildGS-SLAM | 2.52 |
| **DROID-W（Ours）** | **2.30** |

在所有序列上均达到最优或次优，平均 ATE 2.30cm。

#### 2. TUM RGB-D 数据集（ATE RMSE ↓ [cm]）

| 方法 | 平均 ATE |
|------|---------|
| DROID-SLAM | 1.62 |
| WildGS-SLAM | 1.51 |
| **DROID-W（Ours）** | **1.36** |

关键观察：WildGS-SLAM 在低动态序列（f3/sr、f3/shs）上比 DROID-SLAM 更差（不确定性估计错误），而 DROID-W 在低动态和高动态序列上均保持稳定。

#### 3. DyCheck 数据集（ATE RMSE ↓，归一化）

| 方法 | 平均 ATE |
|------|---------|
| DROID-SLAM | 0.044 |
| WildGS-SLAM | 0.056（多序列失败） |
| TTT3R | 0.068 |
| **DROID-W（Ours）** | **0.034** |

WildGS-SLAM 在复杂场景中因重建质量差导致不确定性估计失败，DROID-W 保持鲁棒。

#### 4. DROID-W 数据集（自建大规模室外数据集，ATE RMSE ↓ [m]）

| 方法 | 平均 ATE |
|------|---------|
| TTT3R | 7.309 |
| Splat-SLAM | 1.597 |
| DROID-SLAM | 1.460 |
| WildGS-SLAM | 0.637 |
| **DROID-W（Ours）** | **0.230** |

在自建的大规模室外动态数据集上，DROID-W 以 0.230m 的平均 ATE 大幅领先所有基线。

#### 5. 运行速度（FPS ↑，RTX 3090）

| 方法 | Bonn | TUM | DyCheck |
|------|------|-----|---------|
| DROID-SLAM | 19.89 | 26.97 | 17.50 |
| WildGS-SLAM | 0.22 | 0.32 | 0.18 |
| **DROID-W（Ours）** | **10.57** | **14.92** | **11.06** |

DROID-W 比 WildGS-SLAM **快约 40 倍**，实现实时运行（~10 FPS）。比 DROID-SLAM 略慢（额外的 Metric3D 深度估计 + DINOv2 特征提取）。

---

### 消融实验（Bonn 数据集，ATE RMSE [cm]）

| 配置 | ATE |
|------|-----|
| a. 无不确定性感知 BA | 5.13 |
| b. 无单目深度正则化 | 3.30 |
| c. 无不确定性解耦 | 2.57 |
| d. 无仿射映射 | 2.47 |
| e. 无 weight decay | 2.34 |
| **完整系统** | **2.30** |

- **不确定性感知 BA 最关键**（去掉后 ATE 从 2.30 → 5.13，+123%）
- **单目深度正则化次之**（去掉后 ATE 从 2.30 → 3.30，+43%）
- 不确定性解耦、仿射映射、weight decay 均有贡献

---

### 数据集贡献：DROID-W Dataset

自建大规模室外动态数据集，使用 Livox Mid-360 LiDAR + RGB 相机刚性固连采集：

| 属性 | 说明 |
|------|------|
| 序列数 | 7 个（Downtown 1–7） |
| 分辨率 | 1200×1600 |
| GT 来源 | Downtown 1–2：FAST-LIVO2 轨迹；Downtown 3–7：RTK |
| 附加传感器 | IMU + LiDAR（同步） |
| 场景特点 | 大规模室外，多种动态物体，高度杂乱 |

另外还在 6 个 YouTube 野外视频上测试（8 秒到 30 分钟不等），相机内参用 MonST3R 从 20 帧估计。

---

### 局限性

1. **初始化阶段不确定性不准**：不确定性优化依赖帧间对齐，初始化时位姿估计不可靠，可能导致不确定性估计错误（如 DyCheck 的 haru 序列，移动的狗占据大部分视野，背景特征太少）
2. **多子模型组合**：需要同时运行 DROID 特征提取器 + FiT3D + Metric3D，推理链路较重
3. **仅支持 RGB 输入**（不需要深度传感器，但 Metric3D 提供度量深度先验）

---

### 与本目录其他论文的关系

| 对比维度 | DROID-W | ViPE | VGGT |
|---------|---------|------|------|
| **任务** | 动态场景 SLAM（位姿+重建） | 野外视频位姿估计 | 多视图 3D 重建 |
| **动态处理** | ✅ 核心贡献 | ❌ 假设静态 | ❌ 假设静态 |
| **实时性** | ✅ ~10 FPS | ❌ 3~5 FPS | ✅ 0.2s/场景 |
| **长视频** | ✅ 在线增量处理 | ✅ 滑动窗口 | ❌ 显存随帧数爆炸 |
| **度量尺度** | ✅（Metric3D 提供） | ✅ | ✅ |

---

## UniDepthV2: Universal Monocular Metric Depth Estimation Made Simpler

**发表年份**：2025（arXiv 2502.20110v2，2025.12，ETH Zurich + Toyota Motor Europe）

**代码**：https://github.com/lpiccinelli-eth/UniDepth

### 核心问题

**单目度量深度估计（MMDE）** 的核心挑战：现有方法在训练域内精度很高，但跨域泛化极差——换个相机、换个场景就崩。根本原因：
1. 训练数据相机内参和场景尺度分布窄
2. 大多数方法需要在测试时提供相机内参，限制了通用性
3. GT 深度稀疏且边缘模糊，导致预测结果边界不清晰

**UniDepthV2 的目标**：不依赖任何外部信息（无需相机内参），从单张图像直接预测度量 3D 点云，实现真正的**通用 MMDE**。

---

### 核心设计：4 大创新

#### 1. 伪球面输出表示（Pseudo-Spherical Representation）

**问题**：直接在笛卡尔坐标 $(x, y, z)$ 中预测 3D 点，相机方向和深度耦合在一起，梯度互相干扰。

**解决**：用 **(方位角 θ, 仰角 φ, 对数深度 $z_{\log}$)** 的伪球面坐标表示输出空间：
- $(θ, φ)$ 对应相机射线方向（相机模块负责）
- $z_{\log}$ 对应深度（深度模块负责）
- 两者**正交解耦**，深度误差的梯度不会反传到相机模块

最终 3D 输出 $O = C \| Z$（相机射线与深度的拼接），损失函数：

$$\mathcal{L}_{\lambda\text{MSE}}(\varepsilon) = \|\mathbb{V}[\varepsilon]\|_1 + \lambda^T(\mathbb{E}[\varepsilon] \odot \mathbb{E}[\varepsilon])$$

其中 $\lambda_\theta = \lambda_\phi = 1$，$\lambda_z = 0.15$（等价于 SIlog 损失）。

#### 2. 自提示相机模块（Self-Promptable Camera Module）

**问题**：没有相机内参时，深度模块不知道场景的全局尺度，容易模式崩溃。

**解决**：相机模块从图像自身预测稠密相机表示，作为 Prompt 注入深度模块：

1. 相机模块的 4 个 token（对应 $f_x, f_y, c_x, c_y$ 的残差）经 MLP 解码为针孔相机参数
2. 对每个像素做反投影，得到单位球面上的射线向量 $(r_x, r_y)$
3. 用 **Sine 编码**（64 个谐波，共 128 通道）嵌入射线，得到稠密相机嵌入 $E$
4. 通过**交叉注意力**将 $E$ 注入深度模块的 4 个分辨率特征层：

$$F_i|E = \text{MLP}(\text{CA}(F_i, E))$$

**相比 UniDepth 的改进**：原版用球谐函数（SH）编码，计算量大；V2 改用 Sine 编码，效率提升 2× 以上，性能相当。

#### 3. 几何不变性损失（Geometric Invariance Loss）

**思路**：同一场景从不同相机拍摄，相机条件化后的深度预测应该一致。

**实现**：对每张训练图像做 $N=2$ 次几何增强（随机缩放 $r \sim 2^{U[-2,2]}$ + 随机平移），模拟不同相机拍摄同一场景：

$$\mathcal{L}_{\text{con}}(Z_1, Z_2) = \|\mathcal{T}_2 \circ \mathcal{T}_1^{-1} \circ Z_1 - \text{sg}(Z_2)\|_1$$

双向损失取平均，`sg` 为 stop-gradient。

**V2 的简化**：原版在相机条件化特征 $D|E$ 上计算，V2 直接在输出深度 $Z$ 上计算，效果相当但更简洁（Occam's razor）。

#### 4. 边缘引导归一化损失（Edge-Guided Scale-Shift Invariant Loss, $\mathcal{L}_{\text{EG-SSI}}$）

**问题**：UniDepth 预测的深度边界模糊，细节丢失。

**解决**：在 RGB 梯度最大的 5% 区域（高对比度边缘）提取随机 patch（大小为图像最小边的 4%~8%），对 patch 内深度做局部归一化（减中位数除 MAD），计算预测与 GT 的 L1 损失：

$$\mathcal{L}_{\text{EG-SSI}}(D, D^*, \Omega) = \sum_{\omega \in \Omega} \|\mathcal{N}_\omega(D_\omega) - \mathcal{N}_\omega(D^*_\omega)\|_1$$

**关键设计**：
- 局部归一化使损失只关注形状一致性，不受绝对深度尺度影响
- 惩罚两类错误：① 忽略真实深度不连续；② 在纹理变化处幻觉出假边界
- 用自定义 CUDA kernel 实现 patch 提取，加速 20×

**总损失**：

$$\mathcal{L} = \mathcal{L}_{\lambda\text{MSE}} + 0.1\mathcal{L}_{\text{con}} + 1.0\mathcal{L}_{\text{EG-SSI}} + 0.1\mathcal{L}_{\text{L1}}$$

其中 $\mathcal{L}_{\text{L1}}$ 监督不确定性输出（见下）。

---

### 网络架构

```
输入图像 I
    │
    ├─ ViT Encoder → 4 尺度特征 {F_i}（H/14 × W/14）
    │
    ├─ Camera Module
    │   ├─ 4 个 class token → MLP → 针孔参数残差 (Δfx, Δfy, Δcx, Δcy)
    │   ├─ 反投影 → 单位球面射线 (rx, ry)
    │   └─ Sine 编码 → 稠密相机嵌入 E（H×W×128）
    │
    └─ Depth Module
        ├─ 4 层交叉注意力（每个分辨率一层）：F_i|E = MLP(CA(F_i, E))
        ├─ FPN 风格解码器（含 ResNet blocks + 多分辨率侧连接）
        └─ 输出：
            ├─ 深度 Z_log（对数深度）
            ├─ 相机射线 C（方位角+仰角）
            ├─ 3D 输出 O = C || exp(Z_log)
            └─ 不确定性 Σ（无需额外监督信号，用预测误差自监督）
```

**不确定性输出**：$\mathcal{L}_{\text{L1}} = \|\Sigma - \text{sg}(|Z_{\log} - Z^*_{\log}|)\|_1$，用 stop-gradient 的预测误差作为伪标签，训练时无需额外 GT。

---

### 实验结果

#### 零样本评测（10 个数据集，无任何微调）

**室内数据集（NYU、IBims-1、HAMMER、Booster）**：UniDepthV2-Large 在 δ₁、FA（3D 估计精度）上全面超越所有基线，包括需要 GT 相机内参的 Metric3D 和 Metric3Dv2。

**室外数据集（ETH3D、Sintel、DDAD、NuScenes）**：

| 方法 | 是否需要GT相机 | ETH3D δ₁↑ | DDAD δ₁↑ | NuScenes δ₁↑ |
|------|-------------|-----------|---------|-------------|
| Metric3Dv2 | ✅ 需要 | 90.0 | — | 84.1 |
| DepthPro | ❌ | 39.7 | 29.9 | 56.6 |
| UniDepth | ❌ | 18.5 | 85.8 | 84.6 |
| **UniDepthV2-Large** | ❌ | **85.2** | **88.2** | **87.0** |

**挑战性场景（水下 FLSea、透明/镜面 Booster、HAMMER）**：UniDepthV2-Large 在 FLSea 上 δ₁=90.5，大幅超越所有基线。

#### 边缘清晰度评测（Scale-Invariant Boundary F1）

| 方法 | ETH3D | IBims-1 | Sintel |
|------|-------|---------|--------|
| UniDepth | 0.22 | 2.64 | 0.42 |
| DepthPro† | 4.04 | 19.38 | 29.24 |
| **UniDepthV2-Large** | **2.95** | **13.75** | **33.08** |

注：DepthPro 在固定 1536×1536 高分辨率下推理，UniDepthV2 在原始分辨率下推理，边界 F1 随分辨率提升，因此 UniDepthV2 在公平条件下实际更优。

#### 微调评测（KITTI Eigen-split）

| 方法 | δ₁↑ | A.Rel↓ | RMS↓ |
|------|-----|--------|------|
| DepthAnythingv2 | 98.3 | 4.50 | 1.86 |
| Metric3Dv2 | 98.5 | 4.40 | 1.99 |
| **UniDepthV2-Large** | **98.9** | **3.73** | **1.71** |

KITTI 官方 Depth Prediction Benchmark 排名第一（发布时）。

#### 推理效率（A6000 GPU，0.5MP 图像）

| 方法 | 延迟(ms)↓ | 参数量(M)↓ | FLOPs(T)↓ | 显存(GiB)↓ |
|------|----------|----------|----------|----------|
| UniDepth | 91.0 | 347.0 | 2.02 | 2.81 |
| Metric3Dv2 | 133.6 | 411.9 | 3.47 | 3.50 |
| DepthPro† | 270.6 | 952.0 | 19.3 | 8.42 |
| **UniDepthV2-Small** | **23.0** | **34.18** | **0.29** | **0.66** |
| **UniDepthV2-Large** | **65.4** | **353.8** | **2.17** | **3.47** |

UniDepthV2 比 UniDepth 快约 **1.4×**，比 DepthPro 快约 **4×**，参数量和 FLOPs 大幅减少。

---

### 消融实验关键结论

| 改进点 | δ₁ 变化 | 结论 |
|--------|---------|------|
| SH 编码 → Sine 编码 | 几乎不变 | 效率提升 2×，性能持平 |
| 去掉 Decoder 中的 Attention | -4.3% | 但效率提升 2×，用 ResNet blocks 补偿 |
| 加入 ResNet blocks | +2.3% | 利用局部空间结构 |
| 加入多分辨率特征融合（FPN） | +1.9% | 最终架构与 UniDepth 性能持平但快 3× |
| 加入 $\mathcal{L}_{\text{EG-SSI}}$ | **+4.7% δ₁, +1.8% FA** | 最大单项提升 |
| 伪球面 vs 笛卡尔表示 | **+10.9% δ₁, +19.1% FA** | 解耦相机和深度至关重要 |
| 有无相机条件化 | **+5.2% δ₁** | 相机 Prompt 对 V2 效果更强 |

---

### 不确定性估计

| 模型 | 域内 nAUSE↓ | 域内 ρ↑ | 零样本 nAUSE↓ | 零样本 ρ↑ |
|------|-----------|--------|------------|---------|
| UniDepthV2-Small | 0.221 | 0.682 | 0.539 | 0.289 |
| UniDepthV2-Large | 0.199 | 0.744 | 0.645 | 0.299 |

- 域内：不确定性与误差高度相关（nAUSE 接近 0，ρ≈0.74）
- 零样本：质量下降但仍有信息量（远好于随机 nAUSE=1）
- 大模型在零样本下 nAUSE 反而略高（容量驱动的过拟合：学到了更尖锐的不确定性先验，局部排序准但跨域幅度估计偏差）

---

### 局限性

1. **镜面**：缺乏足够上下文时，镜面被解读为空洞，深度严重变形
2. **非针孔相机**（鱼眼、全景）：相机模块基于针孔假设，无法处理非线性畸变
3. **非写实绘画**（梵高《星夜》等）：几何扭曲的艺术风格导致深度估计混乱
4. **相机参数估计（ρA）提升有限**：训练相机多样性不足，需要额外相机专项训练

---

### 与本目录其他论文的关系

| 对比维度 | UniDepthV2 | DROID-W | VGGT | ViPE |
|---------|-----------|---------|------|------|
| **任务** | 单图度量深度估计 | 动态 SLAM | 多视图 3D 重建 | 视频位姿估计 |
| **输入** | 单张图像 | RGB 视频序列 | 多张图像 | 视频序列 |
| **相机内参** | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 |
| **动态场景** | ❌ 单帧，无时序 | ✅ 核心贡献 | ❌ 假设静态 | ❌ 假设静态 |
| **输出** | 稠密深度图 + 不确定性 | 相机轨迹 + 点云 | 位姿 + 深度 + 点云 | 相机位姿 |
| **实时性** | ✅ 23~65ms/帧 | ✅ ~10 FPS | ✅ 0.2s/场景 | ❌ 3~5 FPS |

**在 DROID-W 中的角色**：UniDepthV2 的前身 Metric3D 被 DROID-W 用作单目深度正则化（$D_t$），为 BA 提供度量尺度先验。UniDepthV2 本身也可以作为更强的深度先验替换 Metric3D。

---

## VGGT-Ω: Scaling Feed-Forward Reconstruction

**发表年份**：2026（arXiv 2605.15195，2026.05，牛津大学 VGG + Meta AI）

**项目页面**：http://vggt-omega.github.io/

### 核心定位

VGGT-Ω 是 VGGT 的大规模升级版，核心问题是：**前馈重建模型能否像 LLM 一样随模型和数据规模可预测地提升性能？**

答案是肯定的——论文展示了从 0.2B 到 10B 参数、从 2K 到 2M 训练序列的幂律式性能提升（图 1）。为了实现这种规模化训练，VGGT-Ω 在架构、数据、训练三个维度对 VGGT 做了系统性改进。

---

### 与 VGGT 的核心区别（重点）

#### 架构改进：3 处关键变化，节省 70% 训练显存

**改进 1：Register Attention（寄存器注意力）**

| | VGGT | VGGT-Ω |
|--|------|--------|
| 全局注意力 | 所有帧的所有 token 互相注意（计算量 $O(N^2 \cdot S^2)$） | 25% 的全局注意力层替换为 Register Attention |
| Register 数量 | 每帧 4 个寄存器 token | 每帧 **16 个**寄存器 token（称为"场景 token"） |
| Register 用途 | 辅助 token，推理时丢弃 | **核心贡献**：聚合全局场景信息，推理时保留并用于下游任务 |

**Register Attention 的工作方式**：
```
每帧 I_i 独立拥有 16 个寄存器 token z_i^scene（不跨帧共享）
N 帧合计 N×16 个寄存器 token

Register Attention 层：
  只有这 N×16 个寄存器 token 之间做跨帧自注意力
  图像 patch token 完全不参与
  → 寄存器聚合全局场景信息

Frame Attention 层（紧随其后）：
  每帧更新后的 16 个寄存器与本帧图像 token 在帧内交互
  → 将全局信息重新分发给图像 token
```

效果：替换 25% 全局注意力层 → 节省 **23% FLOPs + 16% 训练显存**，性能无损失（point error 0.071 vs 0.073）。

**改进 2：简化稠密预测头**

| | VGGT | VGGT-Ω |
|--|------|--------|
| 稠密预测头 | DPT（含高分辨率卷积层，显存占用大） | 保留低分辨率卷积层 + **MLP + pixel-shuffle** 替换高分辨率部分 |
| 输出头数量 | 多个（深度头、点云头、跟踪特征头） | **单个深度头**（点云和跟踪通过损失函数间接监督） |

高分辨率卷积层虽然参数少，但存储前向激活的显存开销极大，且 FSDP/梯度检查点无法消除。改用 MLP + pixel-shuffle 后显存大幅降低。

**改进 3：多任务损失但单头输出**

VGGT 发现多任务训练有益，但多个稠密预测头代价高。VGGT-Ω 的解决方案：
- 只保留**一个深度预测头**
- 点云损失 $\mathcal{L}_{\text{point}}$：从深度 + 相机反投影得到点云，与 GT 比较
- 匹配损失 $\mathcal{L}_{\text{match}}$：对最后一层输出的 token 做对比学习（正对 = 对应同一 3D 位置的 token，负对 = 其他）
- 相机头：轻量 Transformer 对相机 token + 寄存器做一次前向，无迭代精化（VGGT 有迭代精化）

总损失：$\mathcal{L} = \lambda_{\text{cam}}\mathcal{L}_{\text{cam}} + \lambda_{\text{depth}}\mathcal{L}_{\text{depth}} + \lambda_{\text{point}}\mathcal{L}_{\text{point}} + \lambda_{\text{match}}\mathcal{L}_{\text{match}}$

相机损失改用 $\ell_1$（VGGT 用 Huber），更稳定。

#### 其他架构细节差异

| 维度 | VGGT | VGGT-Ω |
|------|------|--------|
| 视觉编码器 | DINOv2（patch size 14） | **DINOv3**（patch size 16，token 数减少 ~25%） |
| 模型规模 | 1.2B | 0.2B / 0.5B / **1B** / **10B** |
| 相机头 | 4 层额外自注意力 + 线性层（迭代精化） | 轻量 Transformer 一次前向，无迭代 |
| 输出 | 相机 + 深度 + 点云 + 跟踪特征（4 个头） | 相机 + 深度（1 个稠密头，其余通过损失监督） |
| 寄存器数量 | 4 个/帧 | **16 个/帧**（场景 token） |

---

### 数据规模化：15× 更多训练数据

#### 公开数据集

收集 30+ 个公开数据集（约 3M 序列），相比 VGGT 的 16 个数据集大幅扩充。**注意**：排除了 VGGT 使用的 Kubric 和 PointOdyssey，因为它们的背景几何是假的，会产生无效深度。

#### 新数据标注流水线（支持动态场景）

针对约 4000 万内部互联网风格视频，构建了一套高质量标注流水线：

```
4000 万视频
    ↓ VLM 预过滤（过滤水印、多镜头、极端运动模糊等）
    → 50% 直接丢弃，40% 标记为可重建但质量存疑，10% 进入下一阶段
    ↓ Grounding DINO 检测动态物体（人、车等），排除动态区域
    ↓ 特征匹配（SIFT + SuperPoint/SuperGlue + ALIKED/LightGlue + VGGSfM Tracker）
    ↓ VGGT 初始化相机参数 → COLMAP Bundle Adjustment
    ↓ 多视图一致性检验（反投影 + 重投影深度比较）
    ↓ 监督几何过滤（XGBoost + 随机森林 + CatBoost 分类器）
    → 最终保留 ~80 万序列（20 万动态 + 60 万静态）
```

**标注质量验证**：在 Sintel 上与 MegaSaM 对比，本流水线 AUC@30°=96.4%，MegaSaM 仅 62.1%，说明标注质量远高于 MegaSaM。

**总训练数据**：公开数据集（3M）+ 新标注数据（0.8M）+ 合成数据 ≈ **4M 序列**，是 VGGT 的 **15× 以上**。

---

### 自监督训练：利用 1800 万无标注视频

受 DINO 启发，采用教师-学生动量蒸馏策略：

```
同一视频序列
    ├─ 学生网络（梯度更新）：随机颜色抖动 + 模糊 + 90°旋转 + patch 遮挡 + 帧重排
    └─ 教师网络（EMA 更新）：独立随机增强

对齐帧顺序后，学生需要匹配教师的：
    ├─ token 特征分布（多层 ℓ2 特征匹配损失）
    └─ 相机和深度预测（回归损失，相机/深度头冻结防止崩溃）
```

效果：在 1800 万无标注视频上训练，提升域外泛化能力，point error 从 0.073 → 0.070。

---

### 实验结果

#### 相机位姿估计（AUC，越高越好）

| 方法 | Sintel AUC@3°↑ | Sintel AUC@30°↑ | ETH3D AUC@3°↑ | DyCheck AUC@3°↑ |
|------|--------------|----------------|--------------|----------------|
| VGGT | 15.0 | 50.0 | 81.7 | 21.0 |
| MegaSaM（优化） | 22.5 | 58.3 | 17.2 | 26.8 |
| DA3（1B） | 16.2 | 52.7 | 86.4 | 32.1 |
| **VGGT-Ω 1B** | 35.3 | 73.0 | 89.7 | 38.4 |
| **VGGT-Ω 10B** | **40.0** | **79.1** | **92.5** | **43.7** |

- Sintel AUC@3° 从 22.5（MegaSaM）→ 40.0，**相对提升 77%**
- ETH3D（宽基线静态场景）从 86.4 → 92.5，MegaSaM 在此仅 17.2（优化方法在宽基线场景崩溃）
- 10B 模型在所有 6 个 benchmark 上均优于 1B 模型，验证了规模化收益

#### 深度估计（δ₁.₂₅，越高越好）

| 方法 | Sintel δ₁.₂₅↑ | ETH3D δ₁.₂₅↑ | DyCheck δ₁.₂₅↑ |
|------|--------------|--------------|----------------|
| VGGT | 79.2 | 97.4 | 95.2 |
| DA3（1B） | 86.1 | 99.6 | 97.7 |
| **VGGT-Ω 1B** | 89.5 | 99.8 | 98.4 |
| **VGGT-Ω 10B** | **93.5** | **99.8** | **98.7** |

Sintel δ₁.₂₅ 从 86.1（DA3）→ 93.5，**相对提升 26%**。

#### 推理速度与显存（单张 80GB A100）

| 方法 | 最大可处理帧数 | 1000 帧推理时间 |
|------|-------------|--------------|
| DA3-Giant | ~750 帧（OOM） | — |
| VGGT（修正后） | ~1250 帧 | 基准 |
| **VGGT-Ω** | **~1250 帧** | **快 20~25%**（DINOv3 patch 16 + Register Attention） |
| VGGT-Ω（纯 Register Attention） | ~1250 帧 | **快 ~20×**（240s → 11.7s），但精度下降至 VGGT 水平 |

---

### 消融实验关键结论

| 配置 | Point Error↓ | 说明 |
|------|-------------|------|
| 纯全局注意力（基线） | 0.071 | 最优精度 |
| **25% Register Attention** | **0.073** | 几乎无损，节省 23% FLOPs |
| 100% Register Attention | ~VGGT 水平 | 精度大幅下降，但速度极快 |
| 去掉点云+匹配损失 | 0.078 | 多任务损失有效（+0.005） |
| VGGT 原始多头多任务 | 0.070 | 最优但显存代价高 |
| 自监督训练（10% 步数） | 0.070 | 轻微提升，主要改善域外泛化 |
| 数据 2K → 2M 序列 | 0.275 → 0.073 | 数据规模最关键 |
| 模型 0.3B → 10B | 0.107 → 0.046 | 模型规模同样重要 |

---

### 寄存器的下游应用（超越重建）

#### 1. 机器人 VLA（视觉-语言-动作模型）

将 VGGT-Ω 的场景 token（寄存器）冻结后拼接到 OpenVLA-OFT 的输入 token 中，在 LIBERO benchmark 上：

| 方法 | 平均成功率 |
|------|---------|
| OpenVLA-OFT（基线） | 96.7% |
| **+ VGGT-Ω 冻结场景 token** | **99.2%** |

几何感知的寄存器显著提升了机器人操作任务的成功率。

#### 2. 语言对齐

用 InfoNCE 对比损失将寄存器与 VLM 生成的场景描述对齐（VLM 冻结，只微调 VGGT-Ω，10K 步）：

| 评测 | Top-1 准确率 | Top-3 准确率 |
|------|------------|------------|
| VLM 嵌入（训练目标） | 76.8% | 97.0% |
| 文本 LLM 嵌入（零样本迁移） | 47.5% | 77.8% |

寄存器携带的高层语义信息可以与语言空间对齐，且对齐后几何任务性能无下降。

#### 3. 运动感知（无监督涌现）

对中间层 token 做 PCA + k-means 聚类（无标签、无光流、无监督探针），发现：
- 早期层（第 4 层）：聚类精确分离运动物体与静态背景
- 中间层（第 13 层）：运动信号减弱但仍可辨
- 深层（第 23 层）：聚类变为语义级别（所有人聚在一起，不区分运动与否）

运动感知作为重建目标的副产品自然涌现，无需显式运动监督。

---

### 局限性与未来方向

1. **相机头无迭代精化**：论文指出加回迭代精化可额外提升 4~6% AUC@3°，但为了简洁性放弃
2. **MLP-only 解码头的 patch 伪影**：纯 MLP 解码在室外远景（天空、山脉）产生块状伪影，保留了少量低分辨率卷积层
3. **自监督训练效果有限**：目前只在域外泛化上有明显收益，在标准 benchmark 上提升有限；动态场景的自监督仍是开放问题
4. **辅助输入（时序、相机参数）在预训练阶段有害**：只在微调阶段引入有效

---

### 与 VGGT 的完整对比总结

| 维度 | VGGT | VGGT-Ω |
|------|------|--------|
| **模型规模** | 1.2B | 0.2B / 0.5B / 1B / 10B |
| **视觉编码器** | DINOv2（patch 14） | DINOv3（patch 16） |
| **寄存器数量** | 4 个/帧（辅助，推理丢弃） | **16 个/帧（场景 token，推理保留）** |
| **全局注意力** | 100% 全局自注意力 | **75% 全局 + 25% Register Attention** |
| **稠密预测头** | 多头（深度+点云+跟踪） | **单头（仅深度）** |
| **相机头** | 4 层自注意力 + 迭代精化 | 轻量 Transformer，单次前向 |
| **训练数据** | ~270K 序列（16 个数据集） | **~4M 序列（30+ 数据集 + 新标注流水线）** |
| **动态场景** | ❌ 不支持 | ✅ 支持（标注流水线含动态场景） |
| **自监督** | ❌ | ✅ 教师-学生动量蒸馏（1800 万无标注视频） |
| **训练显存** | 基准 | **节省 70%** |
| **推理速度** | 基准 | **快 20~25%** |
| **Sintel AUC@3°** | 15.0 | **40.0（+167%）** |
| **寄存器下游应用** | 无 | VLA 机器人 + 语言对齐 + 运动感知 |
